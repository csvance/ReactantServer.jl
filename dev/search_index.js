var documenterSearchIndex = {"docs":
[{"category":"section","location":"manual/client_usage/#Client-Usage","page":"Client Usage","text":"ReactantServerClient is the inference client for a ReactantServer worker or the gateway. It builds KServe V2 requests against ReactantServerCore's protobuf messages and forwards them over gRPC. It depends only on ReactantServerCore and the gRPC layer, so it carries no Reactant/XLA dependency and installs quickly on a plain client machine. (The same KServe V2 wire protocol means any Triton/KServe gRPC client also works; this package is the convenient Julia option.)","title":"Client Usage"},{"category":"section","location":"manual/client_usage/#Installation","page":"Client Usage","text":"The client is a workspace member. From this repository, activate its project:\n\njulia --project=packages/ReactantServerClient -e 'using Pkg; Pkg.instantiate()'\n\nYou can confirm it pulls no Reactant:\n\nusing ReactantServerClient\n@assert Base.identify_package(ReactantServerClient, \"Reactant\") === nothing","title":"Installation"},{"category":"section","location":"manual/client_usage/#One-shot-inference","page":"Client Usage","text":"Point a KServeModel at a running server, build inputs with InferInput, call the one-shot infer_sync, and read outputs with InferOutput:\n\nusing ReactantServerClient\n\nkserve_init()\ntry\n    model = KServeModel(\"grpc://127.0.0.1:8080\", \"scale4\"; max_batch_size = 1)\n    x = Float32[1, 2, 3, 4]\n    response = infer_sync(model, [InferInput(\"INPUT__0\", x)])\n    y = InferOutput(\"OUTPUT__0\", response, Float32)\n    @show vec(collect(y))\nfinally\n    kserve_shutdown()\nend\n\nRun the snippet under the client project against a server you have started (for example a worker on the e2e scale4 bundle).","title":"One-shot inference"},{"category":"section","location":"manual/client_usage/#Batched-inference-over-a-dataset","page":"Client Usage","text":"For many items, implement AbstractInferenceIO and use infer_async (concurrent) or infer_sync(model, io) (serial). The driver stages each chunk through a shared ReactantServerCore.BufferPool: when the server shares the client's IPC namespace, inputs travel over the Triton system shared-memory data plane (zero extra copies on the wire); otherwise the driver transparently falls back to inline transport. Your IO implements length, item_input_bytes, infer_encode_chunk! (write a chunk's inputs into the provided pool slot and return the input descriptors), and infer_decode_chunk! (consume the response). The pool's fixed-slot allocator hands every chunk a disjoint slot, so concurrent infer_async calls are safe.\n\nkserve_init(; pool_bytes, n_slots) sizes the staging pool and the number of slots (the dispatch concurrency); kserve_shutdown() tears it down.\n\nA concrete IO for a model with input INPUT__0 and output OUTPUT__0, each a length-4 Float32 vector per item:\n\nusing ReactantServerClient\n\nstruct VectorIO <: AbstractInferenceIO\n    inputs::Vector{Vector{Float32}}    # each item: 4 Float32\n    outputs::Vector{Vector{Float32}}   # filled in by infer_decode_chunk!\nend\n\nBase.length(io::VectorIO) = length(io.inputs)\n\n# Bytes one item contributes to the request, summed over all inputs.\nReactantServerClient.item_input_bytes(::VectorIO) = 4 * sizeof(Float32)\n\nfunction ReactantServerClient.infer_encode_chunk!(io::VectorIO, r::UnitRange, slot::PoolSlot)\n    n = length(r)\n    sub = subslot(slot, n * 4 * sizeof(Float32))\n    buf = pool_view(sub, Float32, n * 4)\n    @infer_inbounds for (k, i) in enumerate(r)        # hot path: elided normally, checked under validate_io\n        buf[(4k - 3):(4k)] .= io.inputs[i]\n    end\n    # Julia column-major shape: the per-item dims, then the batch axis last.\n    return [InferInput(\"INPUT__0\", sub, [4, n], Float32)]\nend\n\nfunction ReactantServerClient.infer_decode_chunk!(io::VectorIO, r::UnitRange, response)\n    out = InferOutput(\"OUTPUT__0\", response, Float32) # Julia column-major (4, n)\n    @infer_inbounds for (j, i) in enumerate(r)\n        io.outputs[i] = collect(out[:, j])\n    end\n    return nothing\nend\n\nio = VectorIO([Float32[i, i, i, i] for i in 1:100], [Float32[] for _ in 1:100])\ninfer_async(model, io)                                # results land in io.outputs","title":"Batched inference over a dataset"},{"category":"section","location":"manual/client_usage/#Validating-an-IO-against-a-model","page":"Client Usage","text":"Because an IO hard-codes a model's tensor names, dtypes, and shapes, it can silently drift from the served model. validate_io dry-runs an IO against the model's true I/O spec without sending an inference request: it runs infer_encode_chunk! and infer_decode_chunk! once over synthetic, spec-shaped data and surfaces name, dtype, shape, and indexing mismatches as errors. Get the spec from model_io_spec(model) against a running server, or from manifest_io_spec(path) offline against a model's manifest.yaml (suitable for a build-time check). The harness runs your real methods, so call it on a representative or dummy IO.\n\nvalidate_io(model, io)                            # online: fetches the spec from the server\nvalidate_io(manifest_io_spec(\"manifest.yaml\"), io)      # offline: no server needed\n\nFor example, validating the VectorIO above against a model whose OUTPUT__0 is actually a length-3 vector would report the shape mismatch, and an infer_decode_chunk! that indexed out[:, j] past the real batch size would surface as an indexing error, both without sending a request.","title":"Validating an IO against a model"},{"category":"section","location":"manual/client_usage/#Bounds-checks-in-hot-paths","page":"Client Usage","text":"validate_io can only catch an out-of-range index if the access is bounds-checked. Julia's built-in @inbounds elides the check unconditionally, so an @inbounds access stays unchecked even inside the dry run. If you want @inbounds performance in production but a robust check during validation, write the access with @infer_inbounds instead, as the VectorIO example does. It elides the check in normal use, but because validate_io runs your methods inside with_bounds_checks, an @infer_inbounds access is bounds-checked during the dry run and an out-of-range index is caught rather than silently read. This is opt-in: using @infer_inbounds is the user's responsibility, and bare @inbounds remains unchecked unless Julia is started with --check-bounds=yes (which Pkg.test does).\n\n# Bounds-checked under validate_io, elided in production:\n@infer_inbounds buf[(4k - 3):(4k)] .= io.inputs[i]\n\n# Force the checked behavior anywhere, not just in validate_io:\nwith_bounds_checks() do\n    run_my_io_step()\nend\n\n","title":"Bounds checks in hot paths"},{"category":"section","location":"design/philosophy/#Project-Philosophy","page":"Philosophy","text":"This document describes who the project is for, what it optimizes for, and what it will not become. It exists to set expectations for users and contributors, and to serve as a reference when evaluating proposed changes.","title":"Project Philosophy"},{"category":"section","location":"design/philosophy/#Mission","page":"Philosophy","text":"Make serving compiled (non-LLM) models elegant: a hackable, Julia-first inference stack that maximizes the economic efficiency of GPU-based inference, built first for small and mid-size labs.\n\nThe efficiency half is concrete. GPU memory accounts for roughly two thirds of GPU cost; serving infrastructure that wastes memory wastes money. For the foreseeable future that means serving the largest number of models per GPU at a given quality of service. The elegance half is what makes that power usable by a small team: the whole stack is plain Julia, legible end to end, adoptable off the shelf, and open to being bent toward a workload nobody anticipated. The project's purpose is to give the people who care about both arguments the tools to act on them.","title":"Mission"},{"category":"section","location":"design/philosophy/#Who-this-is-for","page":"Philosophy","text":"Small and mid-size labs and engineering organizations who need big-tech-level efficiency without big-tech scale. Concretely, this includes:\n\nStartups and small companies serving production ML where each GPU is a meaningful fraction of infrastructure spend.\nScientific computing and research groups operating within bounded compute budgets.\nOn-premise deployments where hardware is already purchased and underutilization is wasted capacity.\nCloud deployments where GPU-hours are the dominant cost line.\n\nThe common thread is that these organizations measure their bottlenecks, optimize their stacks, and cannot solve serving problems by spending their way out. They benefit from compiler-grade optimization but lack the headcount to build it themselves.\n\nThe audience is engineers and operators who understand what is happening inside their systems. The project gives them tools; it does not abstract those tools beyond recognition or hide the system's behavior behind convenient defaults.\n\nLarger organizations can use it too. There is no reason the project cannot run as a component of a large-scale system, and the control-plane seam exists for exactly that. The difference is in what ships in the box: for small and mid-size labs the project provides streamlined, off-the-shelf deployments; at large scale it provides the seams and leaves the surrounding tooling to the organizations that already build their own.","title":"Who this is for"},{"category":"section","location":"design/philosophy/#One-system,-three-deployment-scales","page":"Philosophy","text":"The mission is served at three deployment scales, and the project treats each as a first-class use case with its own complete answer:\n\nSmall: one GPU, one process. A single worker is the entire deployment. The fair scheduler shares the GPU across models by weighted deficit and learned cost, batch coalescing turns queue depth into throughput, and the on-demand weight cache serves a catalog larger than device memory. The worker speaks the full KServe V2 gRPC surface itself: one Julia process, one YAML file, nothing else to operate.\nMedium: a few GPUs, one extra process. One worker per GPU behind the gateway. In its LPT-packing mode the gateway derives model placement from live measurements, concentrating each model's traffic on few workers so batches actually fill while balancing compute load and weight memory across GPUs. The workers switch to the simpler FIFO discipline and the placement intelligence moves upstream. There is still no external infrastructure to stand up: the gateway is one more Julia process and one more YAML file, and there is no placement file to maintain.\nLarge: bring your own control plane. Workers in explicit mode cede model lifecycle and residency to the operator's own control plane through a small gRPC control surface (status, residency, and policy RPCs), observable through the same KServe readiness APIs and Prometheus metrics the smaller tiers use. The project supplies the seam; the organization supplies the tooling that encodes needs only it can know. The first two tiers are off-the-shelf solutions; this one is deliberately a building block.\n\nThe principle that binds the tiers is non-interference: the project tries to serve these different use cases, but one use case must not crowd out, degrade, or overcomplicate the others. Each tier is strictly additive. A single-GPU deployment never pays for the gateway in dependencies, configuration, or runtime cost; a gateway deployment never requires a control plane; each step up is selected by a configuration value, not a different architecture. A feature that helps one tier is welcome in the form that leaves the other tiers untouched, and suspect in any form that does not.","title":"One system, three deployment scales"},{"category":"section","location":"design/philosophy/#Who-this-is-not-for","page":"Philosophy","text":"Some user populations are better served by other tools. The project does not try to serve them, and being clear about this protects both the project and the users who would otherwise be disappointed.\n\nHyperscale platform requirements. Organizations operating at the scale of thousands of GPUs need multi-tenant isolation, complex traffic shaping, and deep integration with bespoke internal platforms. Large deployments are a supported use case through the control-plane seam described above, which lets an organization bring exactly that machinery; what the project will not do is build it into the core, where every smaller deployment would pay for it.\nLLM serving at scale. vLLM, TGI, TensorRT-LLM, and similar projects are purpose-built for that domain and do it well. This project does not compete in that market.\nUsers who want a packaged solution. This is infrastructure for builders, not a hosted service. Users who do not want to think about the underlying architecture should choose a managed inference service.\nMulti-framework deployments. This project is Reactant-centric: a model must be lowered by Reactant to a device executable first (today via StableHLO/XLA). Teams that need to serve PyTorch, TensorFlow, and ONNX models side by side without converting them are better served by Triton or similar.\nResearch and rapid prototyping workflows. The project is for production serving. Eager-mode debugging, dynamic graphs, and interactive iteration are not what it optimizes for.\n\nIf you fall into any of these categories, the project is not aimed at you, and that is a feature rather than a deficiency.","title":"Who this is not for"},{"category":"section","location":"design/philosophy/#What-the-project-optimizes-for","page":"Philosophy","text":"In approximate priority order:\n\nEconomic efficiency per GPU. Models served per GPU, watts per inference, dollars per million requests. These are the metrics that matter for the target audience.\nPredictable, deterministic resource use. Pre-allocated memory pools, static buffer assignment, no surprise allocations. Predictability matters for regulated deployments and for operators who need to size their hardware confidently.\nCompiler-grade optimization. Whole-program compiler optimization (XLA today), kernel fusion across operations, layout planning. This is the leverage that lets small teams approach big-tech efficiency.\nA hackable, Julia-first stack. The serving path, scheduler, gateway, client, and export tooling are plain Julia. There is no opaque core wrapped in scripting glue: the code a lab reads is the code that runs, so behavior can be inspected with the language's own tools and modified without a second toolchain. Elegance here is not aesthetics; it is the property that makes the system adoptable off the shelf and bendable when the shelf does not fit.\nOperator clarity. Operators should be able to read configuration files, inspect bundles, understand scheduler behavior, and debug failures without reverse-engineering the system. The architecture is legible.\nVerification scope. The codebase is small enough to audit. Dependencies are bounded and stable. The system is suitable for regulated environments without heroic compliance effort.","title":"What the project optimizes for"},{"category":"section","location":"design/philosophy/#What-the-project-explicitly-does-not-optimize-for","page":"Philosophy","text":"Convenience over efficiency. When a choice trades runtime cost for developer convenience, efficiency wins by default. Users who prefer the opposite tradeoff have other tools.\nMaximum generality. The project is opinionated about its design point. It does not try to be the inference server for every use case.\nCompatibility with all upstream frameworks. Models Reactant can trace and lower work (today via StableHLO/XLA); others do not. The conversion tooling makes this boundary as friendly as possible, but the boundary is real.\nFeatureful APIs. The server's API surface is small on purpose. Each addition is weighed against the cost of the cognitive load it imposes on every user, not just the users who would benefit.","title":"What the project explicitly does not optimize for"},{"category":"section","location":"design/philosophy/#What-features-will-not-be-considered","page":"Philosophy","text":"Some categories of changes are off the table, regardless of who proposes them. Stating this explicitly is not standoffish; it is honest about the project's identity. Users and potential contributors deserve to know what to expect.\n\nScale-specific features in the shared core. A feature that serves one deployment scale belongs behind the seam that scale already uses (a configuration switch, the gateway, the control-plane RPCs), not in the core where every other deployment carries its weight. A feature useful at the scale of thousands of GPUs but with no benefit at the scale of one to fifty is better implemented in the control plane an organization brings; the project will extend that seam before it will absorb the feature.\n\nFeatures that abstract the project to death. Every layer of abstraction has a cost: cognitive load on users, complexity in the codebase, surface area for bugs, longer onboarding for new contributors. The project will not add layers of abstraction that primarily exist to support uncommon use cases. The cost falls on everyone; the benefit accrues to a few.\n\nFeatures that add significant cognitive load for minimal benefit. Configuration options that require deep understanding to set correctly, APIs that have many ways to express the same thing, behaviors that depend on subtle interactions between settings. If a feature's correct use requires extensive documentation or expertise that most users will not have, the feature is suspect even if it is technically sound.\n\nFeatures driven by users who have not otherwise contributed. This is not a hard rule, and well-argued proposals from any source will be considered on their merits. But proposals from organizations that benefit from the project without contributing to it are weighed against proposals from contributors who have done the work. The project's direction is set by people who care about the same problem, not by feature requests from anonymous bystanders.\n\nFeatures that compromise the core mission for marginal users. Every project gets pulled toward feature accumulation by users with edge cases. The project will resist this. Features that work against the economic efficiency goal will not be added even if individual users would find them convenient. The integrity of the mission is more valuable than any single feature.","title":"What features will not be considered"},{"category":"section","location":"design/philosophy/#What-the-project-will-consider","page":"Philosophy","text":"Proposals are evaluated against the mission, not against the proposer's preferences. A few categories of contributions are particularly welcome:\n\nImprovements to economic efficiency: better memory packing, lower-overhead scheduling, faster startup, smaller binaries.\nNew conversion paths from upstream frameworks to StableHLO, especially for model architectures the project does not yet handle well.\nDocumentation and tooling that make the project easier to evaluate and adopt for the target audience.\nBenchmarks against alternative inference servers on realistic workloads.\nBug reports and fixes, especially for edge cases discovered in production deployments.\nPerformance regressions, especially with reproducers. The project takes performance as a feature, not as an afterthought.\n\nContributions are weighed against the mission. Changes that improve efficiency, reduce cognitive load, or make the project more useful to the target audience are likely to land. Changes that move in the opposite direction are unlikely to, regardless of how well-implemented they are.\n\n","title":"What the project will consider"},{"category":"section","location":"api/runtime/#Runtime-and-Weights","page":"Runtime & Weights","text":"The device-side runtime: weight loading and materialization, the on-demand GPU weight cache, and the shared device memory pool. These are internal interfaces, documented here for operators and contributors. See On-demand Weights for the operational view.","title":"Runtime & Weights"},{"category":"section","location":"api/runtime/#Weights","page":"Runtime & Weights","text":"","title":"Weights"},{"category":"section","location":"api/runtime/#Weight-stores-(host-RAM)","page":"Runtime & Weights","text":"","title":"Weight stores (host RAM)"},{"category":"section","location":"api/runtime/#Weight-cache","page":"Runtime & Weights","text":"","title":"Weight cache"},{"category":"section","location":"api/runtime/#Memory-pool","page":"Runtime & Weights","text":"","title":"Memory pool"},{"category":"method","location":"api/runtime/#ReactantServer.free_weights!-Tuple{ReactantServer.AbstractBackend, Any}","page":"Runtime & Weights","text":"free_weights!(backend, bufs) -> nothing\n\nRelease every device buffer in bufs. Used to evict an unpinned model's resident weights.\n\n\n\n\n\n","title":"ReactantServer.free_weights!"},{"category":"method","location":"api/runtime/#ReactantServer.host_materialize-Tuple{PrivateWeightStore, Any, Any, Vector{String}}","page":"Runtime & Weights","text":"host_materialize(store, key, st, names) -> Vector{Any}\n\nMaterialize a model's host weight floor through a WeightStore. The private store allocates per-worker arrays (identical to materialize_host_weights); the shared store backs them with a node-shared SHM region so same-node workers share one copy. key is the model name.\n\n\n\n\n\n","title":"ReactantServer.host_materialize"},{"category":"method","location":"api/runtime/#ReactantServer.host_release!-Tuple{PrivateWeightStore, Any}","page":"Runtime & Weights","text":"host_release!(store, key) -> nothing\n\nRelease a model's host weight floor previously obtained from host_materialize. A no-op for the private store; for the shared store it detaches and, if last on the node, unlinks the region. The caller must drop its references to the host arrays first.\n\n\n\n\n\n","title":"ReactantServer.host_release!"},{"category":"method","location":"api/runtime/#ReactantServer.materialize_host_weights-Tuple{Any, Vector{String}}","page":"Runtime & Weights","text":"materialize_host_weights(st, names) -> Vector{Any}\n\nMaterialize the named weights from the lazy mmap safetensors views into resident host Arrays, in weightnames order. This is the expensive step (a layout copy at host-memory speed); doing it once at startup and keeping the result resident in RAM lets an on-demand GPU load be a pure host->device transfer (see `transferto_device`) rather than a re-materialization.\n\n\n\n\n\n","title":"ReactantServer.materialize_host_weights"},{"category":"method","location":"api/runtime/#ReactantServer.transfer_to_device-Tuple{ReactantServer.AbstractBackend, ReactantServer.MemoryPool, Vector{Any}}","page":"Runtime & Weights","text":"transfer_to_device(backend, pool, hosts) -> Vector{Any}\n\nTransfer already-materialized host weight Arrays to device buffers, in order. This is the only cost paid on an on-demand load when weights are pinned in host RAM.\n\n\n\n\n\n","title":"ReactantServer.transfer_to_device"},{"category":"method","location":"api/runtime/#ReactantServer.weight_order-Tuple{Any}","page":"Runtime & Weights","text":"weight_order(st) -> Vector{String}\n\nReturn weight names in StableHLO argument order. Reads the argumentorder field from the safetensors metadata (a JSON-encoded list). A file with tensors but no argumentorder is an error; a file with no tensors yields an empty order.\n\n\n\n\n\n","title":"ReactantServer.weight_order"},{"category":"method","location":"api/runtime/#ReactantServer.weights_nbytes-Tuple{Any, Vector{String}}","page":"Runtime & Weights","text":"weights_nbytes(st, names) -> Int\n\nTotal device footprint of the named weights, summed from the lazy safetensors views' shape and element type. Reads only metadata (no collect), so it is cheap and lets the weight cache budget against a model before its weights are ever loaded.\n\n\n\n\n\n","title":"ReactantServer.weights_nbytes"},{"category":"type","location":"api/runtime/#ReactantServerCore.PrivateWeightStore","page":"Runtime & Weights","text":"PrivateWeightStore()\n\nThe default store: each worker materializes its own host copy of a model's weights.\n\n\n\n\n\n","title":"ReactantServerCore.PrivateWeightStore"},{"category":"type","location":"api/runtime/#ReactantServerCore.SharedWeightStore","page":"Runtime & Weights","text":"SharedWeightStore(; mode=0o666)\n\nOpt-in store backing each model's host weights with a node-level POSIX shared-memory region so same-node workers share one copy. See the file header for the flock-coordinated protocol. mode sets the permission bits for the regions and their lock files. The default 0o666 lets workers running as unrelated UIDs (for example different containers) share the regions, but is world-writable; 0o660 is recommended for production and multi-user systems.\n\n\n\n\n\n","title":"ReactantServerCore.SharedWeightStore"},{"category":"type","location":"api/runtime/#ReactantServerCore.WeightStore","page":"Runtime & Weights","text":"WeightStore\n\nHost-RAM residency backend for a model's materialized weights. PrivateWeightStore (the default) gives each worker its own copy; SharedWeightStore shares one copy per model across same-node workers through a POSIX shared-memory region. See the file header for the coordination protocol.\n\n\n\n\n\n","title":"ReactantServerCore.WeightStore"},{"category":"method","location":"api/runtime/#ReactantServerCore.materialize_host_weights!-Tuple{PrivateWeightStore, Vararg{Any, 4}}","page":"Runtime & Weights","text":"materialize_host_weights!(store, key, digest, specs, fill!) -> Vector{Any}\n\nReturn a model's host weight Arrays (in weight order), populating them via fill!(arrays) when this worker is the one that must materialize them. specs is a vector of (eltype, dims) per tensor. For PrivateWeightStore the arrays are freshly allocated; for SharedWeightStore they alias a node-shared region.\n\n\n\n\n\n","title":"ReactantServerCore.materialize_host_weights!"},{"category":"method","location":"api/runtime/#ReactantServerCore.release_host_weights!-Tuple{PrivateWeightStore, Any}","page":"Runtime & Weights","text":"release_host_weights!(store, key) -> nothing\n\nRelease a model's host weights. For SharedWeightStore this detaches the region and, if this was the last holder on the node (a non-blocking upgrade to an exclusive flock succeeds), unlinks the region and its lock file. A no-op for the private store. The caller must drop all references to the arrays first.\n\n\n\n\n\n","title":"ReactantServerCore.release_host_weights!"},{"category":"method","location":"api/runtime/#ReactantServerCore.weights_digest-Tuple{AbstractString, Any}","page":"Runtime & Weights","text":"weights_digest(key, specs) -> UInt64\n\nContent digest over a model's identity and weight layout (name, per-tensor dtype/size/shape, and a format version). Two workers computing this for the same model agree on the same region key.\n\n\n\n\n\n","title":"ReactantServerCore.weights_digest"},{"category":"type","location":"api/runtime/#ReactantServer.NotResidentError","page":"Runtime & Weights","text":"NotResidentError\n\nRaised by acquire! in externally-managed mode when a request targets a model whose weights are not currently resident. A control plane is authoritative for residency in that mode, so the worker does not autonomously load; the model must be pinned first.\n\n\n\n\n\n","title":"ReactantServer.NotResidentError"},{"category":"method","location":"api/runtime/#ReactantServer.acquire!-Tuple{ReactantServer.WeightCache, ReactantServer.ModelEntry}","page":"Runtime & Weights","text":"acquire!(cache, entry) -> nothing\n\nGuarantee entry.executable.weights is resident before the model runs. Device-pinned and already-resident models return immediately (the latter is bumped to most-recently-used). In self-managed mode an evicted model is loaded autonomously, evicting LRU victims until it fits the budget (a model larger than the whole budget is loaded anyway after evicting everything, with a warning, since it cannot run otherwise). In externally-managed mode the worker does not autonomously load: an evicted model raises NotResidentError.\n\n\n\n\n\n","title":"ReactantServer.acquire!"},{"category":"method","location":"api/runtime/#ReactantServer.preload_pinned!-Tuple{ReactantServer.WeightCache, ReactantServer.ModelRegistry}","page":"Runtime & Weights","text":"preload_pinned!(cache, registry) -> nothing\n\nEnsure every pinned model's weights are resident. build_loaded_model already loads them when on-demand mode is on, so this is normally a no-op; it loads defensively otherwise.\n\n\n\n\n\n","title":"ReactantServer.preload_pinned!"},{"category":"method","location":"api/runtime/#ReactantServer.release_all!-Tuple{ReactantServer.WeightCache, ReactantServer.ModelEntry}","page":"Runtime & Weights","text":"release_all!(cache, entry) -> nothing\n\nRelease every residency a model holds: free its device buffers, drop it from the LRU budget, and release any shared host floor. Used by evict! when a model is removed from the worker. Runs on the dispatch thread (sole residency mutator); the device free runs outside the lock.\n\n\n\n\n\n","title":"ReactantServer.release_all!"},{"category":"method","location":"api/runtime/#ReactantServer.set_residency_state!-Tuple{ReactantServer.WeightCache, ReactantServer.ModelEntry, ResidencyState}","page":"Runtime & Weights","text":"set_residency_state!(cache, entry, target) -> ResidencyState\n\nMove a model to the target residency floor (see ResidencyState). Like acquire! this runs on the dispatch-loop thread, the sole mutator of residency. It materializes or drops the host floor and, for PINNED_DEVICE, ensures the weights are resident on the device (exempt from the budget). In externally-managed mode, leaving the device floor releases the device buffers (there is no autonomous evictor to reclaim them); in self-managed mode they stay resident but become evictable. The slow host/device work runs outside the lock; only the bookkeeping commit is locked.\n\n\n\n\n\n","title":"ReactantServer.set_residency_state!"},{"category":"method","location":"api/runtime/#ReactantServer.weight_cache_stats-Tuple{ReactantServer.WeightCache}","page":"Runtime & Weights","text":"weight_cache_stats(cache) -> NamedTuple\n\nSnapshot the cache counters under its lock for observability.\n\n\n\n\n\n","title":"ReactantServer.weight_cache_stats"},{"category":"section","location":"manual/getting_started/#Getting-Started","page":"Getting Started","text":"This is a complete walkthrough: export a small Lux model into a bundle, configure a single-GPU node, run it (both with docker compose and from pure Julia), and query it with the Julia client. When you are ready for more than one GPU, continue to Scaling to Multiple GPUs.\n\nThe commands below target a CUDA GPU host (backend: cuda, --gpus all). The runtime is device agnostic, so the same steps work on CPU by setting backend: cpu and dropping the GPU flags; that is handy for following along without a GPU.","title":"Getting Started"},{"category":"section","location":"manual/getting_started/#Installation","page":"Getting Started","text":"ReactantServer is a Julia workspace of five packages under packages/ (ReactantServerCore, ReactantServer, ReactantServerGateway, ReactantServerClient, ReactantServerNode), plus the non-member ReactantServerExport for offline bundle export (see Architecture). It vendors its forked/unregistered dependencies (Reactant, gRPCServer, gRPCClient, HTTP) as git submodules under lib/ and wires them in through the workspace [sources]. After cloning, populate the submodules and instantiate the workspace:\n\ngit submodule update --init --recursive\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\n\nTo work in a single package, activate its project instead, e.g. julia --project=packages/ReactantServer.","title":"Installation"},{"category":"section","location":"manual/getting_started/#Step-1:-Export-a-model-into-a-bundle","page":"Getting Started","text":"A served model is a bundle: a directory with a manifest.yaml, a compiled StableHLO program (model.mlir), and its weights.safetensors. Bundles are produced offline by ReactantServerExport, which is not a workspace member (it carries Lux/PythonCall weak dependencies that the server image should not), so use its ready test environment:\n\njulia --project=packages/ReactantServerExport/test\n\nExport a tiny Lux MLP. The batch dimension is the last Julia axis (the Lux convention), so a 4-feature input is (4, batch):\n\nusing Lux, ReactantServerExport, Random\n\nmodel = Lux.Chain(Lux.Dense(4 => 8, tanh), Lux.Dense(8 => 3))\nps, st = Lux.setup(Random.Xoshiro(0), model)\n\nexample = randn(Float32, 4, 1)            # (features, batch)\nReactantServerExport.export_bundle(:lux, model, ps, st, example;\n    dir = joinpath(\"models\", \"mlp\"), name = \"mlp\", batch_sizes = [1])\n\nThis writes models/mlp/: a manifest.yaml, the compiled StableHLO module (one per batch size, here model.b1.mlir), and weights.safetensors. The input tensor is named input and the output output by default (override with input_name / output_name); the client snippets below use those names. models/ is now a model repository: every immediate subdirectory with a manifest.yaml is a servable model, keyed by its directory name (mlp here). See Bundles & model.jl for the manifest format and custom pre/post-processing.","title":"Step 1: Export a model into a bundle"},{"category":"section","location":"manual/getting_started/#Step-2:-Configure-a-single-GPU-node","page":"Getting Started","text":"A deployment is described by one node file. The minimal single-GPU node needs only the model repository, a base port, the runtime backend, and one worker:\n\n# node.yaml\nmodel_repo: /var/lib/reactantserver/models\nbase_port: 8080\nmetrics_base_port: 9100\n\nglobal:\n  runtime:\n    backend: cuda         # use \"cpu\" to follow along without a GPU\n  endpoints:\n    host: 0.0.0.0\n\nworkers:\n  - { name: worker0 }     # one worker on the (single) GPU\n\nThe explicit one-entry workers: list works with every run path below, including a bare ReactantServer.serve (which expects the workers list). Under the supervisor you can instead omit workers: and write gpus: auto, and it synthesizes one worker per detected GPU; that is how the image's baked default and Scaling to Multiple GPUs work. See Node Configuration for the full surface (scheduler, on-demand weights, per-model pinning, environment overrides).","title":"Step 2: Configure a single-GPU node"},{"category":"section","location":"manual/getting_started/#Step-3:-Run-it-with-docker-compose","page":"Getting Started","text":"Build the image and start the node, pointing REACTANTSERVER_MODELS at the repository from Step 1:\n\nmake image\nREACTANTSERVER_MODELS=$PWD/models docker compose up\n\nWith a single GPU the node runs one worker and no gateway: the worker serves the KServe V2 gRPC API on localhost:8001 and metrics/health on localhost:8002 (/readyz, /healthz, /metrics). The first start compiles every model before accepting traffic, so give it a moment; curl localhost:8002/readyz returns 200 once it is serving. See Docker Deployment for the image, healthcheck, and metrics details.","title":"Step 3: Run it with docker compose"},{"category":"section","location":"manual/getting_started/#Step-4:-Or-run-it-from-pure-Julia","page":"Getting Started","text":"Two entry points, differing only in which ports are exposed:\n\n# The supervisor: same behavior as the container. One worker (no gateway) on the public\n# ports 8001 (gRPC) and 8002 (metrics), just like `docker compose up`.\nusing ReactantServerNode\nReactantServerNode.supervise(\"node.yaml\")\n\n# A single bare worker, no supervisor: serves on the node file's own port (base_port, 8080).\nusing ReactantServer\nReactantServer.serve(\"node.yaml\")          # blocks; Ctrl-C to stop\n\nserve loads the node file, brings up the runtime, compiles the worker's bundles, starts the Scheduler, and finally starts the gRPC server so traffic is accepted only once models are live. Pass blocking=false to get a RunningServer you can stop!:\n\nserver = ReactantServer.serve(\"node.yaml\"; blocking=false)\n# ... issue requests ...\nReactantServer.stop!(server)\n\nsupervise is the right choice for deployment (it is what the container runs and it scales to many GPUs unchanged); a bare serve is convenient for a quick single-worker REPL session.\n\nStart Julia multithreaded so per-request preprocess/postprocess overlap the GPU execution (julia --threads=auto,1: a default pool for the hooks plus one interactive thread for the GPU dispatch loop). Set this yourself only for a bare serve; under the supervisor (the container image) each worker is instead sized to its share of the host, min(cores ÷ workers, 16) threads plus the interactive one, so co-located workers do not oversubscribe the CPU (see Scaling to Multiple GPUs). With a single thread the server still works, just without the overlap.","title":"Step 4: Or run it from pure Julia"},{"category":"section","location":"manual/getting_started/#Step-5:-Query-it","page":"Getting Started","text":"The server speaks KServe V2 over gRPC, so any Triton/KServe client works; this repository ships the Reactant-free ReactantServerClient. Point it at the port your server is using (8001 for the supervisor or compose, 8080 for a bare serve), and use the bundle's tensor names input / output:\n\nusing ReactantServerClient\n\nkserve_init()\ntry\n    model = KServeModel(\"grpc://127.0.0.1:8001\", \"mlp\"; max_batch_size = 1)\n    x = Float32[1, 2, 3, 4]                       # one 4-feature item\n    response = infer_sync(model, [InferInput(\"input\", x)])\n    y = InferOutput(\"output\", response, Float32)  # length-3 output\n    @show vec(collect(y))\nfinally\n    kserve_shutdown()\nend\n\nSee Client Usage for batched inference over a dataset, IO validation, and the shared-memory data path.","title":"Step 5: Query it"},{"category":"section","location":"manual/getting_started/#Next-steps","page":"Getting Started","text":"Common Use Cases: choose a deployment shape (single GPU, multi-GPU distributed or replicated, multi-node) with an example configuration for each.\nScaling to Multiple GPUs: add GPUs and how the supervisor decides what to start.\nNode Configuration: the full config surface and environment overrides.\nBundles & model.jl: the bundle format and custom pre/post-processing.\nOn-demand Weights: serving more models than fit in GPU memory.\nDocker Deployment: the image, roles, health, and metrics.","title":"Next steps"},{"category":"section","location":"manual/getting_started/#Testing","page":"Getting Started","text":"Each package is tested in its own environment; all tests run on CPU and need no GPU:\n\njulia --project=packages/ReactantServerCore   -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServer        -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServerGateway -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServerClient  -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServerNode    -e 'using Pkg; Pkg.test()'\n\n","title":"Testing"},{"category":"section","location":"api/gateway/#Gateway","page":"Gateway","text":"ReactantServerGateway is the KServe V2 gRPC reverse proxy that fronts the workers of a multi-GPU node. The node supervisor runs it as an embedded child when there is more than one worker. It depends only on ReactantServerCore and the gRPC/HTTP layer, never on Reactant. See Multi-GPU Gateway for the operational view.\n\n","title":"Gateway"},{"category":"function","location":"api/gateway/#ReactantServerGateway.serve_gateway","page":"Gateway","text":"serve_gateway(gateway_path=nothing; blocking=true) -> nothing | RunningGateway\n\nLoad gateway.yml (listen addresses and the worker endpoint list), build the worker client pool, start the admin HTTP server and the readiness/discovery prober (which probes each endpoint's ServerReady and RepositoryIndex and swaps in the discovered routing table), and serve the KServe gRPC proxy. When blocking is false the server runs in the background and a RunningGateway is returned (stop it with stop!).\n\ngateway_path may be omitted (or nothing) to configure the gateway from defaults and REACTANT_GATEWAY_* environment variables alone; the endpoint list then comes from REACTANT_GATEWAY_WORKERS. The node supervisor uses this to run an embedded gateway without a gateway.yml.\n\n\n\n\n\n","title":"ReactantServerGateway.serve_gateway"},{"category":"function","location":"api/gateway/#ReactantServerGateway.probe_worker_ready","page":"Gateway","text":"probe_worker_ready(node_path, worker=nothing) -> Bool\n\nResolve a worker's port from the node file and call its KServe ServerReady on localhost, returning whether it reported ready. Used as the worker container's healthcheck (a Julia replacement for the former Go reactant-healthprobe). worker may be omitted when the node has a single worker.\n\n\n\n\n\n","title":"ReactantServerGateway.probe_worker_ready"},{"category":"type","location":"api/gateway/#ReactantServerGateway.RunningGateway","page":"Gateway","text":"RunningGateway\n\nHandle to a gateway started with serve_gateway(...; blocking=false). Pass it to stop! to shut the gRPC server, the readiness prober, and the admin HTTP server down.\n\n\n\n\n\n","title":"ReactantServerGateway.RunningGateway"},{"category":"function","location":"api/gateway/#ReactantServerGateway.stop!","page":"Gateway","text":"stop!(g::RunningGateway)\n\nShut down a gateway started with serve_gateway(...; blocking=false): close the gRPC server, halt the readiness prober, and close the admin HTTP server.\n\n\n\n\n\n","title":"ReactantServerGateway.stop!"},{"category":"section","location":"manual/object_detection/#Object-Detection-(GeneralizedRCNN)","page":"Object Detection","text":"A two-stage object detector is the canonical case for a meta model. A torchvision Faster R-CNN (torchvision.models.detection.fasterrcnn_resnet50_fpn, an FPN backbone + RPN + RoIHeads GeneralizedRCNN) is mostly dense tensor math, but the middle of the pipeline is not: selecting RPN proposals, running NMS, pooling ROI features with roi_align, decoding boxes, and the final per-class NMS are all data-dependent and cannot be captured by torch.export as a single static graph. The shape of the work changes with the contents of the tensors, which is exactly what a static StableHLO program cannot express.\n\nThe package ships a converter that handles this split for you. It produces two dense StableHLO bundles for the parts that trace cleanly and a meta bundle whose model.jl runs the data-dependent glue in Julia between them. The reusable detection math (NMS, roi_align, box decode, anchor generation) lives in the ReactantServer.DetectionGlue module and is already part of the package, so the generated model.jl is small and the conversion is config-driven.\n\nThis page walks through converting a standard torchvision detector end to end. For the underlying execution model (the meta gate, committed sub-calls, placement) see Meta Models.\n\ntip: Runnable example\nexamples/object_detection/ in the repository is a complete, runnable version of this walkthrough. It is split into three copy-pasteable commands, each with its own environment: export the bundles, serve them on a single GPU, and a client that sends an image and draws the detections back onto it with CairoMakie (detections.jpg). See examples/object_detection/README.md.","title":"Object Detection (GeneralizedRCNN)"},{"category":"section","location":"manual/object_detection/#The-shape-of-the-conversion","page":"Object Detection","text":"One source model is emitted as three bundles under the output root:\n\nBundle Contents Kind\n<name>_stage1 Backbone + RPN head StableHLO\n<name>_stage2 Box head + box predictor over a fixed number of ROIs StableHLO\n<name> Chains stage1 → DetectionGlue → stage2 → final NMS meta\n\nstage1 takes the preprocessed image and returns 14 dense tensors: the four ROI-pooling feature maps (feat_0–feat_3), the five per-level objectness maps (obj_0–obj_4), and the five per-level box deltas (delta_0–delta_4). stage2 takes ROI-pooled features for a fixed K proposals ([K, 256, 7, 7] in torch, (7, 7, 256, K) in the Julia column-major wire layout) and returns cls_logits ([K, num_classes]) and bbox_deltas ([K, num_classes*4]). The meta bundle owns no weights of its own; it is placed as a group with its two stages and routed by the gateway as a single unit (see the placement section of Meta Models).\n\nOnly <name> is addressable by clients. The two stages are internal to the meta and never appear in the gateway's routing table.\n\nCrucially, both stages are plain nn.Modules that torch.export traces directly: the converter builds the torchvision model, wraps backbone + rpn.head and box_head + box_predictor, and exports them. There is no torch.jit load and no reaching into a scripted graph's frozen internals.","title":"The shape of the conversion"},{"category":"section","location":"manual/object_detection/#Running-the-converter","page":"Object Detection","text":"The converter is tools/convert_to_stablehlo.jl, driven by a YAML config. The torchvision detector is a handler (a special-case builder), shipped at tools/handlers/torchvision_frcnn_detector.jl. Reference it from the handlers: block of your config, keyed by a model name:\n\noutput_root: /docker/reactantserver/models\n\nhandlers:\n  - file: handlers/torchvision_frcnn_detector.jl\n    models: [my_detector]\n    options:\n      weights: DEFAULT      # DEFAULT = pretrained COCO; \"none\" = random; or a path to a state_dict .pth\n      num_classes: 91       # classes INCLUDING background; set when loading a custom head\n      image_size: 640       # canonical square input edge, divisible by 64\n      input_dtype: u8       # client image dtype (u8 | f32)\n      output_cols: 6        # 5 = [box4, score]; 6 = [box4, score, class]\n\nRelative paths (including file: and any option key ending in _dir/_path) resolve against the config file's directory. With weights: DEFAULT the converter builds the pretrained COCO model, so no source artifact is needed (the runnable demo path); to convert your own trained detector, point weights at a saved state_dict and set num_classes to match its head. A handler runs after the torch/torchax/triton imports, so it may freely call pyexec/pyimport.\n\nRun it from the repository root, instantiating against an environment that has torch, torchvision, torchax, and ReactantServerExport:\n\njulia tools/convert_to_stablehlo.jl <config>.yaml --only my_detector\n\nUse --dry-run to validate the config and handler load without paying torch startup, and --force to rebuild a bundle that already exists. The run emits my_detector_stage1, my_detector_stage2, and the my_detector meta bundle.","title":"Running the converter"},{"category":"section","location":"manual/object_detection/#The-generated-meta-model.jl","page":"Object Detection","text":"The handler bakes the per-model config it reads from the live model (the per-level cell_anchors, the RPN/ROI box-coder weights, the RPN pre-NMS top-k and NMS threshold, and the final score/nms/detections_per_img) into the meta bundle's model.jl, then registers the orchestration with register_meta_model. The emitted function, lightly abridged, is:\n\nconst _G = ReactantServer.DetectionGlue\n\nfunction _run(inputs, call)\n    iw = size(inputs[1].data, 1); ih = size(inputs[1].data, 2)\n\n    # Stage 1: backbone + RPN head. 14 dense outputs.\n    s1 = call(\"my_detector_stage1\", inputs)\n    d  = Dict(t.name => t.data for t in s1)\n\n    # Per-level: generate anchors, decode RPN deltas to boxes, flatten objectness.\n    bl = Matrix{Float64}[]; sl = Vector{Float64}[]\n    for i in 1:5\n        O = d[_OBJ[i]]; D = d[_DEL[i]]\n        anc = _G.generate_anchors(size(O, 2), size(O, 1), _STR[i], _CELL[i])\n        push!(bl, _G.decode_boxes(_G.deltas_matrix(D), anc, _RPNW))\n        push!(sl, _G.objectness_flat(O))\n    end\n\n    # Select the top-K proposals (NMS across levels), then ROIAlign the feature maps. torchvision's\n    # detection pooler is aligned=false with sampling_ratio=2.\n    pb = _G.select_rpn_proposals(bl, sl, ih, iw; pre=_PRE, post=_K, nms_thresh=_RPNNMS); Kp = size(pb, 1)\n    feats = [_G.feature_chw(d[f]) for f in _FEAT]\n    roi = call.scratch((7, 7, 256, _K), Float32); fill!(roi, 0f0)   # reuse buffer; passed by reference\n    lv = [_G.assign_level(@view pb[k, :]) for k in 1:Kp]\n    for l in 0:3\n        sel = findall(==(l), lv); isempty(sel) && continue\n        _G.roi_align_wire!(view(roi, :, :, :, sel), feats[l+1], pb[sel, :], _SCALES[l+1];\n                           ratio=2, aligned=false)\n    end\n\n    # Stage 2: box head + predictor on the pooled ROIs, then the final per-class NMS. torchvision's\n    # FastRCNNPredictor places background at class 0, so bg_first=true.\n    s2 = call(\"my_detector_stage2\", [ReactantServer.NamedTensor(\"ROI_FEATS\", roi)])\n    d2 = Dict(t.name => t.data for t in s2)\n    cls = permutedims(d2[\"cls_logits\"], (2, 1))[1:Kp, :]\n    dl  = permutedims(d2[\"bbox_deltas\"], (2, 1))[1:Kp, :]\n    bx, sc, cl = _G.fast_rcnn_inference(cls, dl, pb, ih, iw;\n        score_thresh=_SCORE, nms_thresh=_NMS, topk=_TOPK, weights=_ROIW, bg_first=true, min_size=1e-2)\n\n    return [ReactantServer.NamedTensor(\"OUTPUT__0\", assemble(bx, sc, cl))]\nend\n\nregister_meta_model(\"my_detector\"; run = _run)\n\nEvery data-dependent step is a plain function in ReactantServer.DetectionGlue: generate_anchors, decode_boxes, select_rpn_proposals, roi_align_wire!, assign_level, and fast_rcnn_inference. A few DetectionGlue knobs select the torchvision conventions: roi_align_wire!(...; aligned=false) uses torchvision's ROIAlign offset and malformed-ROI clamp, and fast_rcnn_inference(...; bg_first=true, min_size=1e-2) treats class 0 as background and drops sub-pixel final boxes the way torchvision's postprocess_detections does. The ROI feature tensor is the large intermediate handed between stages, so it is allocated from the worker's reuse pool with call.scratch; in a fleet that buffer is backed by a shared-memory slot so the sub-call sends it by reference instead of serializing it (see the call.scratch section of Meta Models).","title":"The generated meta model.jl"},{"category":"section","location":"manual/object_detection/#Options-and-assumptions","page":"Object Detection","text":"Option Default Meaning\nweights DEFAULT DEFAULT builds the pretrained COCO model; none is random (structure only); a path loads a state_dict .pth.\nnum_classes 91 Classes including background; set this when loading a custom head.\nimage_size 640 Canonical square input edge, divisible by 64 (FPN p6/pool stride).\ninput_dtype u8 Client image dtype: u8 (divided by 255 then normalized) or f32 (assumed already in [0,1]).\noutput_cols 6 Per-detection width: 5 = [box4, score], 6 = [box4, score, class].\ninput_shapes — Optional list of [W, H] pairs to compile stage1 for several aspect ratios sharing one weight set (each edge divisible by 64).\n\nTwo assumptions are worth calling out:\n\nInput is a batched RGB image. The client sends an NCHW image ([1, 3, H, W]) at the compiled size; stage1 bakes the ImageNet normalization (and the /255 for u8) so the client sends a raw image. The served wrapper letterboxes to one of the compiled image_size/input_shapes; the model is not resized at run time.\nClass ids follow torchvision. With output_cols: 6 the emitted class is the torchvision label (1..num_classes-1); background (class 0) is dropped.","title":"Options and assumptions"},{"category":"section","location":"manual/object_detection/#See-also","page":"Object Detection","text":"examples/object_detection/ for the runnable end-to-end example (export, serve, client + drawing)\nMeta Models for the execution model, gating, deadlines, and placement\nBundles & model.jl for the plain bundle path and the manifest encoding\nregister_meta_model in the API reference\n\n","title":"See also"},{"category":"section","location":"manual/common_use_cases/#Common-Use-Cases","page":"Common Use Cases","text":"This page describes the most common ways people deploy ReactantServer, organized by the situation you are in rather than by feature. Each section describes who the configuration is for, what it provides today, and gives an example configuration to start from.\n\nSupport for more accelerators is planned. CUDA is supported today, with CPU for development and fallback. For the authoritative and current feature details, see the Architecture and the manual pages linked throughout; this page is a guide to choosing a configuration, not a substitute for the reference documentation.","title":"Common Use Cases"},{"category":"section","location":"manual/common_use_cases/#Choosing-a-configuration","page":"Common Use Cases","text":"The decision comes down to how many GPUs you have and, if more than one, whether your constraint is memory or compute.\n\nflowchart TD\n    Start[How many GPUs?] -->|One| Single[Single GPU]\n    Start -->|More than one| Multi{Can every model<br/>fit resident on<br/>every GPU?}\n    Multi -->|No, memory-constrained| Distributed[Multi-GPU, distributed<br/>without replication]\n    Multi -->|Yes, compute-constrained| Replicated[Multi-GPU, replicated]\n    Single -.scale up.-> Distributed\n    NeedControl[Need to coordinate<br/>many nodes?] --> MultiNode[Multi-node:<br/>bring your own control plane]\n\n    classDef box fill:#e6f3ff,stroke:#0066cc,stroke-width:2px,color:#000\n    classDef decision fill:#fff9e6,stroke:#cc9900,stroke-width:2px,color:#000\n    class Single,Distributed,Replicated,MultiNode box\n    class Multi,Start,NeedControl decision\n\nSituation Configuration Optimizes for\nOne GPU, many models Single GPU Fitting many models on limited hardware\nSeveral GPUs, models do not all fit everywhere Multi-GPU distributed Serving more models than any one GPU holds\nSeveral GPUs, models fit everywhere, need throughput Multi-GPU replicated Spreading compute load across replicas\nMany machines Multi-node Whatever your control plane decides","title":"Choosing a configuration"},{"category":"section","location":"manual/common_use_cases/#Single-GPU","page":"Common Use Cases","text":"Who it is for. A small lab that has trained many models and wants to serve them all on one GPU without worrying about whether they fit in VRAM at once.\n\nWhat it provides today.\n\nOn-demand model loading (weights stay resident in host RAM, stream onto the GPU on demand, evicted LRU under a byte budget)\nBatch coalescing\nFIFO and fair (deficit-weighted, cost-aware) scheduling options\nAdd, remove, and update models without restarting the server\n\nOn-demand loading is what makes this work for \"a ton of models\": a card serves far more models than fit in VRAM at once, paying a single host-to-device transfer on a cold call. Fair scheduling ensures that frequently called models do not starve infrequently called ones. Together they give a balanced experience where you can serve a large model library on modest hardware and trust that every model gets served.\n\nThis is the recommended starting point for small labs. It is also the conceptual foundation for the multi-GPU distributed case, which scales this same idea across cards.\n\nExample configuration. A node file with a single worker on the visible GPU. The on-demand weight cache (weight_cache_bytes) lets the model library exceed VRAM: weights load lazily and evict LRU within the budget. The fair discipline shares GPU time across models so a busy model does not starve the rest. Mount it over /etc/reactantserver/node.yaml.\n\n# node.yaml — one GPU, many models served on demand.\nmodel_repo: /var/lib/reactantserver/models\nbase_port: 8080\nmetrics_base_port: 9100\ngpus: auto                          # one worker on the visible GPU\n\nglobal:\n  cache_dir: /var/cache/reactantserver\n  runtime:\n    backend: cuda\n    mem_fraction: 0.9\n    # Weights load lazily and evict LRU within this GPU byte budget (here 24 GiB), so the model\n    # library need not fit in VRAM at once. 0 keeps every weight resident.\n    weight_cache_bytes: 25769803776\n  scheduler:\n    discipline: fair                # cost-aware share of GPU time across models (the default)\n    ema_halflife_seconds: 30.0\n    max_queue_depth: 1024\n  endpoints:\n    host: 0.0.0.0\n\nSee Node Configuration and On-demand Weights for the full surface.","title":"Single GPU"},{"category":"section","location":"manual/common_use_cases/#Multi-GPU,-distributed-without-replication","page":"Common Use Cases","text":"Who it is for. A lab that has outgrown one GPU. You have several cards, but your model library is large enough that you cannot fit every model on every GPU. Your constraint is memory: there is not enough VRAM to replicate everything.\n\nWhat it provides today.\n\nOn-demand model loading (per GPU, as in the single-GPU case)\nReactantServerGateway LPT packing with batch coalescing\nAdd, remove, and update models without restarting the server\n\nYou can think of this as seamlessly scaling up the single-GPU case. The gateway uses LPT (longest-processing-time) packing to distribute models across GPUs, balancing memory footprint against compute load. It does not provide the same guarantees as the single-GPU fair scheduler, but the way LPT packing distributes models implicitly avoids the situation where infrequently called models are completely starved by frequently called ones: spreading models across cards by their load keeps any one card from being monopolized.\n\nflowchart LR\n    Client[Clients] -->|KServe V2 gRPC| Gateway[ReactantServerGateway<br/>LPT packing]\n    Gateway --> W0[Worker GPU 0<br/>models A, B]\n    Gateway --> W1[Worker GPU 1<br/>models C, D]\n    Gateway --> W2[Worker GPU 2<br/>models E, F]\n\n    classDef gw fill:#ccffcc,stroke:#006600,stroke-width:2px,color:#000\n    classDef worker fill:#e6f3ff,stroke:#0066cc,stroke-width:2px,color:#000\n    classDef client fill:#fff9e6,stroke:#cc9900,stroke-width:2px,color:#000\n    class Gateway gw\n    class W0,W1,W2 worker\n    class Client client\n\nExample configuration. A node file with one worker per GPU, and the embedded gateway in lpt_packing mode with default_replicas: 1 so each model lives on a single GPU that the packer chooses by balancing memory and compute. lpt_packing requires the workers to run the fifo discipline (placement moves to the gateway).\n\n# node.yaml — several GPUs on one machine; each model placed on one GPU by the gateway.\nmodel_repo: /var/lib/reactantserver/models\nbase_port: 8080\nmetrics_base_port: 9100\ngpus: auto                          # one worker per visible GPU\n\nglobal:\n  cache_dir: /var/cache/reactantserver\n  runtime:\n    backend: cuda\n    mem_fraction: 0.9\n    weight_cache_bytes: 25769803776 # on-demand cache per GPU, as in the single-GPU case\n  scheduler:\n    discipline: fifo                # required by lpt_packing (placement moves to the gateway)\n    ema_halflife_seconds: 30.0\n    max_queue_depth: 1024\n  endpoints:\n    host: 0.0.0.0\n\nThe supervisor runs the embedded gateway for you; enable lpt_packing on it either with REACTANT_GATEWAY_SCHEDULING_MODE=lpt_packing (plus matching REACTANT_GATEWAY_SCHEDULING_* variables) or by mounting a gateway.yml with:\n\n# gateway.yml — embedded gateway scheduling block.\nscheduling:\n  mode: lpt_packing\n  default_replicas: 1               # each model on a single GPU; the packer balances which one\n  rebalance_compute_seconds: 30     # repack after the fleet consumes this many GPU-seconds\n\nSee Multi-GPU Gateway and Scaling to Multiple GPUs.","title":"Multi-GPU, distributed without replication"},{"category":"section","location":"manual/common_use_cases/#Multi-GPU,-replicated","page":"Common Use Cases","text":"Who it is for. You have several GPUs and enough memory that your models fit on more than one card. Your constraint is compute, not memory: you want to spread a model's request load across replicas for throughput.\n\nWhat it provides today.\n\nReactantServerGateway lpt_packing with a configurable replica count per model and coalescing-aware routing across those replicas\nThe simpler round_robin and least_outstanding modes, which also distribute a model's requests across its replicas when you do not need batch coalescing or reactive placement\n\nWhich scheduling mode. All three modes work with replicas, so the choice is about what you optimize. Use lpt_packing when you want to maximize batch coalescing even with replicas: it concentrates a model's requests to fill one replica's batch before spilling to the next, and it places models on GPUs reactively by measured compute and memory load, including which GPUs host a replicated model. If you do not need reactive model placement (beyond the simple, request-level reactivity that least_outstanding itself provides) or batch coalescing, round_robin and least_outstanding are both significantly less complicated and need no measurements, no fifo discipline, and no startup preconditions. Both route across the replicas the workers already serve, so you replicate a model by loading it on more than one worker rather than by setting a gateway replica count: round_robin spreads requests blindly across those replicas, and least_outstanding spreads them by live occupancy, sending each request to the replica with the fewest in flight.\n\nHow to configure it. For the batching-first path, set scheduling.mode: lpt_packing in gateway.yml and give the replicated model a replica count, either with scheduling.default_replicas or per model under scheduling.models.<name>.replicas. The gateway places each model on that many distinct GPUs and routes its requests to fill one replica's batch before moving to the next, so coalescing is preserved across replicas rather than diluted. Tune routing_fill_factor (raise it above 1.0 to keep the next batch queued under fast arrival) and choose a routing_policy: fill_rr (default) round-robins which replica opens each batch, and fill_least opens it on the least compute-loaded GPU (best when replicas share GPUs with other models). For the simpler path, set scheduling.mode: round_robin or least_outstanding and load the model on each worker that should serve it; neither mode takes a replica count or any of the lpt_packing knobs. See Multi-GPU Gateway for the full set of knobs.\n\nReplica counts are fixed at startup; a model does not fan out automatically under load. Size the replica count for the model's expected concurrency, and rely on the per-GPU queue and batch coalescing to absorb bursts on each replica.\n\nExample configuration. The node file is the same as the distributed case (one worker per GPU, discipline: fifo). The difference is in the gateway: give the hot model a replica count so it is placed on more than one GPU, and the gateway routes its requests to fill one replica's batch before the next.\n\n# gateway.yml — front the node's workers and replicate hot models for throughput.\nscheduling:\n  mode: lpt_packing\n  default_replicas: 1               # most models on one GPU\n  rebalance_compute_seconds: 30\n  routing_fill_factor: 1.0          # raise above 1 to keep the next batch queued under bursty load\n  routing_policy: fill_rr           # fill_least when replicas share GPUs with other models\n  models:\n    resnet50:\n      replicas: 2                   # served on 2 distinct GPUs; requests routed to fill batches\n\nTo replicate every model across all GPUs without listing each one, set default_replicas: all (it resolves to the current ready-worker count and tracks the fleet as workers come and go):\n\nscheduling:\n  mode: lpt_packing\n  default_replicas: all             # every model on every GPU\n\nReplica counts are fixed at startup; size them for the model's expected concurrency.\n\nwarning: Make sure you have the memory to replicate\nReplicating a model puts its full weight footprint on every GPU it lands on. It is your responsibility to keep the result feasible: if the models placed on a GPU do not fit in its weight budget, that worker thrashes, loading and evicting weights on nearly every request and collapsing throughput. This is the failure mode to watch for with default_replicas: all on a memory-constrained fleet. Size replica counts against your GPU memory, and watch the gateway's oversubscription warning (see Multi-GPU Gateway).\n\nSee Multi-GPU Gateway.","title":"Multi-GPU, replicated"},{"category":"section","location":"manual/common_use_cases/#Multi-node-(bring-your-own-control-plane)","page":"Common Use Cases","text":"Who it is for. You are coordinating many machines, beyond what a single node supervisor handles. At this scale you typically have specific requirements (your own service discovery, scheduling policy, failover, traffic management) that a general-purpose control plane would not satisfy well.\n\nWhat the project provides. A KServe V2 gRPC control plane service endpoint. The project deliberately does not ship a multi-node control plane of its own. The reasoning: anyone who needs multi-node coordination is usually at a scale where they would build a control plane tailored to their exact requirements, so a generic one would not fit. Instead, the project provides the interface a control plane integrates against, so you can build or adapt your own coordination layer on top of ReactantServer nodes.\n\nflowchart TD\n    CP[Your control plane<br/>discovery, scheduling, failover] -->|KServe V2 gRPC| N0[ReactantServer node 0]\n    CP --> N1[ReactantServer node 1]\n    CP --> N2[ReactantServer node 2]\n    N0 -.->|each node: one or more GPUs| G0[GPUs]\n    N1 -.-> G1[GPUs]\n    N2 -.-> G2[GPUs]\n\n    classDef cp fill:#ffe6cc,stroke:#cc6600,stroke-width:2px,color:#000\n    classDef node fill:#e6f3ff,stroke:#0066cc,stroke-width:2px,color:#000\n    classDef gpu fill:#f0f0f0,stroke:#888,stroke-width:1px,color:#000\n    class CP cp\n    class N0,N1,N2 node\n    class G0,G1,G2 gpu\n\nThe endpoint contract. Each node exposes the same interface a control plane integrates against: the KServe V2 gRPC data plane (ModelInfer, RepositoryIndex, ServerReady) plus the worker control RPCs (ModelControlStatus, SetModelResidency, SetModelPolicy) for residency and policy, and an admin HTTP port serving /healthz, /readyz, and Prometheus /metrics. Your control plane discovers which models each node serves via RepositoryIndex and routes ModelInfer to a node that reports the model ready.\n\nExample integration. The simplest multi-node setup reuses the standalone gateway, pointed at every node's worker endpoints; a larger deployment replaces it with your own control plane speaking the same gRPC interface.\n\n# gateway.yml — a standalone gateway fronting the workers of several nodes.\nlisten:\n  grpc: \"0.0.0.0:8001\"\n  metrics: \"0.0.0.0:8002\"\nscheduling:\n  mode: lpt_packing\n  default_replicas: 1\nendpoints:\n  - \"node1-worker0:8080\"\n  - \"node1-worker1:8081\"\n  - \"node2-worker0:8080\"\n  - \"node2-worker1:8081\"\n# Optional: aggregate every worker's /metrics behind the gateway's own.\n# metrics_endpoints:\n#   - \"node1-worker0:9100\"\n#   - \"node1-worker1:9101\"\n#   - \"node2-worker0:9100\"\n#   - \"node2-worker1:9101\"\n\nSee Multi-GPU Gateway and Docker Deployment for the node interface, ports, and health/metrics endpoints a control plane would use.","title":"Multi-node (bring your own control plane)"},{"category":"section","location":"manual/common_use_cases/#Summary","page":"Common Use Cases","text":"The common path is to start on a single GPU with on-demand loading and fair scheduling, then scale to multi-GPU distributed (LPT packing) as the model library outgrows one card. Replicated multi-GPU is for compute-bound workloads: set a per-model replica count under lpt_packing and the gateway routes across replicas to preserve batch coalescing. Multi-node is for users who bring their own control plane and integrate against the provided gRPC interface.\n\nFeature support evolves; check the linked manual and design pages for the current state rather than relying on this overview alone.\n\n","title":"Summary"},{"category":"section","location":"api/client/#Client","page":"Client","text":"ReactantServerClient is the inference client for talking to a ReactantServer worker or the gateway over KServe V2 gRPC. It depends only on ReactantServerCore and the gRPC layer, so it carries no Reactant dependency and installs on a plain client machine. See Client Usage for a worked example.\n\n","title":"Client"},{"category":"type","location":"api/client/#ReactantServerClient.AbstractInferenceIO","page":"Client","text":"AbstractInferenceIO\n\nInterface for streaming a dataset through batched inference with infer_async / infer_sync. A concrete subtype implements length(io), item_input_bytes(io), infer_encode_chunk!(io, range, slot) (stage a chunk's inputs into the pool slot and return the input descriptors), and infer_decode_chunk!(io, range, response) (consume the response). The pool owns the staging bytes; the IO must not retain slot references past infer_encode_chunk!.\n\nA subtype may optionally implement output_specs to read its outputs back through shared memory. The default is empty, which keeps every output inline (the response carries raw_output_contents as before). Declaring outputs is transparent to infer_decode_chunk!: the driver reads the shared-memory results back into the response before handing it over, so InferOutput works the same on either transport.\n\n\n\n\n\n","title":"ReactantServerClient.AbstractInferenceIO"},{"category":"type","location":"api/client/#ReactantServerClient.KServeModel","page":"Client","text":"KServeModel(host, port, model_name; secure=false, max_batch_size=1, deadline=10.0, ...)\nKServeModel(url, model_name; max_batch_size=1, deadline=10.0, ...)\n\nA handle to one model served by a KServe V2 gRPC endpoint (a ReactantServer worker or the gateway). The second form parses a url of the form grpc://host:port (or host:port); grpcs/https selects a secure channel. max_batch_size caps how many items the batched infer_async / infer_sync drivers coalesce per request; deadline is the per-request timeout in seconds. max_send_message_length / max_receive_message_length bound a single gRPC message (default 256 MiB, matching the gateway's limits).\n\n\n\n\n\n","title":"ReactantServerClient.KServeModel"},{"category":"type","location":"api/client/#ReactantServerClient.OutputSpec","page":"Client","text":"OutputSpec(name, dtype, per_item_dims)\n\nDeclares an expected network output so the driver can read it back through shared memory. name is the output tensor name, dtype its element type, and per_item_dims its Julia column-major shape for a single item, excluding the leading batch dimension. For each chunk the driver reserves sizeof(dtype) * prod(per_item_dims) * items_in_chunk bytes in the staging slot and asks the server to write that output there. See output_specs.\n\n\n\n\n\n","title":"ReactantServerClient.OutputSpec"},{"category":"method","location":"api/client/#ReactantServerClient.InferInput-Union{Tuple{T}, Tuple{String, AbstractArray{T}}} where T<:Union{Bool, Float16, Float32, Float64, Int16, Int32, Int64, Int8, UInt16, UInt32, UInt64, UInt8}","page":"Client","text":"InferInput(name, array) -> ModelInferRequest.InferInputTensor\n\nBuild a wire input tensor named name from a Julia array, shipping the bytes inline. You pass the array in its natural Julia column-major shape (W, H, …, N); the client reverses it to the network's row-major (N, …, H, W) internally (the bytes are unchanged). Pass a vector of these to the one-shot infer_sync(model, inputs). Variants taking an explicit Julia column-major shape and a typed contents vector are also provided.\n\n\n\n\n\n","title":"ReactantServerClient.InferInput"},{"category":"method","location":"api/client/#ReactantServerClient.InferOutput-Union{Tuple{T}, Tuple{String, ReactantServerCore.inference.ModelInferResponse, Type{T}}} where T<:Union{Bool, Float16, Float32, Float64, Int16, Int32, Int64, Int8, UInt16, UInt32, UInt64, UInt8}","page":"Client","text":"InferOutput(name, response, dtype) -> Array\nInferOutput(name, response) -> Array\n\nExtract the output tensor named name from a ModelInferResponse as a Julia array. The wire row-major shape (N, …, H, W) is reshaped (no copy) to Julia column-major (W, …, N). Pass the element type as dtype for a type-stable result; the two-argument form reads the dtype from the response metadata.\n\n\n\n\n\n","title":"ReactantServerClient.InferOutput"},{"category":"method","location":"api/client/#ReactantServerClient.infer_async-Tuple{AbstractInferenceModel, AbstractInferenceIO}","page":"Client","text":"infer_async(model, io::AbstractInferenceIO)\n\nRun inference over every item in io, staging inputs through the shared-memory [BufferPool] and dispatching chunks concurrently (bounded by the pool's slot count). Each chunk acquires a disjoint slot, so this is safe to call from multiple threads against one model. Results are delivered through io's infer_decode_chunk!. Use infer_sync for serial dispatch.\n\n\n\n\n\n","title":"ReactantServerClient.infer_async"},{"category":"method","location":"api/client/#ReactantServerClient.infer_sync-Tuple{AbstractInferenceModel, AbstractInferenceIO}","page":"Client","text":"infer_sync(model, io::AbstractInferenceIO)\ninfer_sync(model, network_inputs) -> ModelInferResponse\n\nSynchronous inference. The AbstractInferenceIO form drives io one chunk at a time (no concurrency). The second form is a one-shot call: network_inputs is a vector of wire tensors built with InferInput, sent inline in a single ModelInferRequest, and the decoded ModelInferResponse is returned for reading with InferOutput.\n\n\n\n\n\n","title":"ReactantServerClient.infer_sync"},{"category":"method","location":"api/client/#ReactantServerClient.kserve_init-Tuple{}","page":"Client","text":"kserve_init(; pool_bytes=DEFAULT_POOL_BYTES, n_slots=DEFAULT_POOL_SLOTS)\n\nInitialize the gRPC subsystem and (re)set the staging-pool parameters. n_slots is the number of fixed-size slots each pool is divided into, which bounds how many chunks can be in flight concurrently against a single pool.\n\n\n\n\n\n","title":"ReactantServerClient.kserve_init"},{"category":"method","location":"api/client/#ReactantServerClient.kserve_shutdown-Tuple{}","page":"Client","text":"kserve_shutdown()\n\nTear down the client: unregister and unlink the shared-memory pool from every server it was registered with, drop the cached pools and per-URL routes, and shut the gRPC subsystem down. Pair with kserve_init.\n\n\n\n\n\n","title":"ReactantServerClient.kserve_shutdown"},{"category":"method","location":"api/client/#ReactantServerClient.manifest_io_spec-Tuple{AbstractString}","page":"Client","text":"manifest_io_spec(path) -> ModelIOSpec\n\nLoad a model's input/output spec from a manifest YAML at path, with no running server. Reuses the server's own wire encoding (encode_model_metadata), so the result matches model_io_spec for the same model. Suitable for an offline precompile or build-time check. The reported shapes are Julia column-major (batch axis last), so they read in the same axis order as the manifest's einsum letters.\n\n\n\n\n\n","title":"ReactantServerClient.manifest_io_spec"},{"category":"method","location":"api/client/#ReactantServerClient.model_io_spec-Tuple{KServeModel}","page":"Client","text":"model_io_spec(model::KServeModel) -> ModelIOSpec\n\nFetch a model's input/output spec from a running server over the ModelMetadata RPC. Throws if the server is unreachable or does not implement ModelMetadata; the call is explicit, so failing loudly is intended. Pair with validate_io or use it to introspect a model's I/O directly. The reported shapes are Julia column-major (batch axis last), matching the order you build arrays in; the row-major KServe wire shape is reversed away by the client.\n\n\n\n\n\n","title":"ReactantServerClient.model_io_spec"},{"category":"method","location":"api/client/#ReactantServerClient.output_specs-Tuple{AbstractInferenceIO}","page":"Client","text":"output_specs(io) -> Vector{OutputSpec}\n\nOutputs an AbstractInferenceIO declares for shared-memory read-back. Defaults to empty, which keeps every output inline (raw_output_contents) exactly as before: no output declaration means no shared memory, the safe fallback. Returning a non-empty vector opts the IO into shared-memory outputs and explicit-output mode: the request asks the server for exactly these outputs in this order, so every output the IO consumes must be declared. Outputs with data-dependent (dynamic) shapes cannot be sized ahead of time and must stay inline (leave them out, which means returning empty here).\n\n\n\n\n\n","title":"ReactantServerClient.output_specs"},{"category":"method","location":"api/client/#ReactantServerClient.validate_io-Tuple{ModelIOSpec, AbstractInferenceIO}","page":"Client","text":"validate_io(spec::ModelIOSpec, io::AbstractInferenceIO; items=1)\nvalidate_io(model::KServeModel, io::AbstractInferenceIO; items=1)\n\nDry-run io against a model's true I/O spec without sending an inference request. Runs the user's infer_encode_chunk! and infer_decode_chunk! (see AbstractInferenceIO) once for the first items items against a synthetic, spec-shaped request and response, checking input/output names, dtypes, and shapes and surfacing indexing or shape errors in the user's own code as exceptions.\n\nspec comes from manifest_io_spec (offline) or model_io_spec (online); the KServeModel form fetches it online. The harness runs the user's real methods, so it has side effects (it may write into the io's buffers at positions 1:items); call it on a representative or dummy io. Zeroed synthetic data does not exercise data-dependent branches.\n\nThe dry run runs the user methods inside with_bounds_checks, so indexing written with @infer_inbounds is bounds-checked here even though it elides in normal use. Bare @inbounds is not affected by that context; to catch out-of-bounds in bare-@inbounds code, start Julia with --check-bounds=yes (which Pkg.test does).\n\n\n\n\n\n","title":"ReactantServerClient.validate_io"},{"category":"method","location":"api/client/#ReactantServerClient.with_bounds_checks-Tuple{Any}","page":"Client","text":"with_bounds_checks(f)\n\nRun f with forced bounds checking enabled for any @infer_inbounds blocks reached during the call (task-local, restored on exit). validate_io uses this so the dry run honors bounds checks even where the IO would normally elide them.\n\n\n\n\n\n","title":"ReactantServerClient.with_bounds_checks"},{"category":"macro","location":"api/client/#ReactantServerClient.@infer_inbounds-Tuple{Any}","page":"Client","text":"@infer_inbounds expr\n\nLike @inbounds, but the elision is conditional: outside a with_bounds_checks context the wrapped expr runs with bounds checks elided (as @inbounds); inside one it runs with bounds checks. Use it instead of @inbounds in infer_encode_chunk! / infer_decode_chunk! so a validate_io dry run still catches out-of-bounds indexing. Wrap a whole loop or block so the runtime branch is taken once, not per element.\n\nfunction ReactantServerClient.infer_decode_chunk!(io::MyIO, r, response)\n    out = InferOutput(\"OUTPUT__0\", response, Float32)\n    @infer_inbounds for (j, i) in enumerate(r)\n        io.results[i] = collect(out[:, j])\n    end\n    return nothing\nend\n\n\n\n\n\n","title":"ReactantServerClient.@infer_inbounds"},{"category":"section","location":"api/server/#Server-and-Lifecycle","page":"Server & Lifecycle","text":"The top-level entry points for starting and stopping a worker, plus the bundle registration hook called from a model's model.jl.","title":"Server & Lifecycle"},{"category":"section","location":"api/server/#Node-supervisor","page":"Server & Lifecycle","text":"ReactantServerNode.supervise is the container entry point: it detects the visible GPUs, spawns one serve worker subprocess per device, multiplexes the children's output with [name] line prefixes, and restarts them on failure. With two or more workers it also runs the embedded gateway; with a single worker it binds that worker to the public ports directly and runs no gateway. See Docker Deployment for the operational surface.\n\n","title":"Node supervisor"},{"category":"function","location":"api/server/#ReactantServer.serve","page":"Server & Lifecycle","text":"serve(node_path; worker=nothing, backend=ReactantBackend(), blocking=true) -> nothing | RunningServer\n\nLoad the node config at node_path, resolve this process's worker, bring up the runtime and its assigned models, and start the gRPC control plane. worker selects which worker entry to serve; it may be omitted when the node has exactly one worker. When blocking is false the server runs in the background and a RunningServer is returned (stop it with stop!).\n\n\n\n\n\n","title":"ReactantServer.serve"},{"category":"function","location":"api/server/#ReactantServer.serve_worker","page":"Server & Lifecycle","text":"serve_worker(node_path, worker; backend=ReactantBackend(), blocking=true)\n\nConvenience alias for serve that names the worker positionally.\n\n\n\n\n\n","title":"ReactantServer.serve_worker"},{"category":"function","location":"api/server/#ReactantServer.stop!","page":"Server & Lifecycle","text":"stop!(s::RunningServer)\n\nShut down a server started with serve(...; blocking=false). Stops the model-directory watcher (if running), closes the metrics endpoint (if running) and the gRPC server, halts the scheduler's dispatch loop, and tears down any registered shared-memory regions. Returns nothing.\n\n\n\n\n\n","title":"ReactantServer.stop!"},{"category":"function","location":"api/server/#ReactantServer.register_model","page":"Server & Lifecycle","text":"register_model(name; preprocess=identity, postprocess=identity)\n\nCalled from a bundle's model.jl to register custom pre/post-processing. Both hooks receive and return a Vector{NamedTensor}. Omitted hooks default to identity.\n\n\n\n\n\n","title":"ReactantServer.register_model"},{"category":"function","location":"api/server/#ReactantServer.register_meta_model","page":"Server & Lifecycle","text":"register_meta_model(name; run)\n\nCalled from a meta bundle's model.jl to register the orchestration function. run has the form run(inputs::Vector{NamedTensor}, call) -> Vector{NamedTensor}, where call(model_name, inputs) invokes another model. The meta runs as a scheduled unit holding the GPU exclusively, and call invokes the sub-model's compiled executable directly in-process (no queue re-entry, no gateway hop).\n\n\n\n\n\n","title":"ReactantServer.register_meta_model"},{"category":"type","location":"api/server/#ReactantServer.RunningServer","page":"Server & Lifecycle","text":"RunningServer\n\nHandle to a server started with serve(...; blocking=false). It holds the resolved ServerConfig, the model registry, the running Scheduler, the device memory pool, the shared-memory registry, the underlying gRPC server, and the listen port. Pass it to stop! to shut the server down.\n\n\n\n\n\n","title":"ReactantServer.RunningServer"},{"category":"function","location":"api/server/#ReactantServerNode.supervise","page":"Server & Lifecycle","text":"supervise(node_path; role=nothing, gateway_path=nothing, sink=stdout, env=ENV,\n          install_signal_handlers=true, kwargs...) -> Int\n\nRun the node: spawn one worker subprocess per visible GPU (and the embedded gateway in the default all-in-one role), multiplex their output onto sink with [name] line prefixes, restart children that die, and block until SIGTERM/SIGINT (or a crash-loop budget breach). Returns the process exit code.\n\n\n\n\n\n","title":"ReactantServerNode.supervise"},{"category":"section","location":"api/config/#Configuration","page":"Configuration","text":"The typed configuration a worker resolves from its node file. These types live in ReactantServerCore (the shared substrate) and are re-exported by ReactantServer. See Node Configuration for how these map onto the YAML.","title":"Configuration"},{"category":"section","location":"api/config/#Node-files","page":"Configuration","text":"Parsing and validation of the node file, and the resolution of one worker's ServerConfig from it:\n\n","title":"Node files"},{"category":"type","location":"api/config/#ReactantServerCore.ServerConfig","page":"Configuration","text":"ServerConfig\n\nThe fully resolved configuration for a single worker process, frozen for the process lifetime. It is produced from a node file (see node.jl) with environment-variable overrides applied, then checked by validate_config. Fields: model_dirs (bundle search paths), cache_dir, the RuntimeConfig, SchedulerConfig, and EndpointsConfig sub-configs, models_include (an allowlist of model names to load; empty means load all), model_poll_seconds (the dynamic-mode interval at which the worker re-scans its model_dirs for added, changed, or removed bundles and hot-swaps them), and model_control_mode (see ModelControlMode: static, dynamic, or explicit). ReactantServer.serve also accepts a ServerConfig directly.\n\n\n\n\n\n","title":"ReactantServerCore.ServerConfig"},{"category":"type","location":"api/config/#ReactantServerCore.RuntimeConfig","page":"Configuration","text":"RuntimeConfig\n\nRuntime and device settings (the runtime: config block). backend selects CPU or CUDA execution; device_ordinal picks the GPU among several visible ones; mem_fraction is the fraction of device memory claimed for the pool; preallocate claims that pool up front; allow_cpu_fallback permits falling back to CPU when the device is unavailable; weight_cache_bytes is the GPU byte budget for on-demand (unpinned) weights (0 keeps every model's weights resident, the original behavior); residency_mode selects self-managed or externally-managed residency; shared_host_weights opts the node into the shared-memory host-weight store so same-node workers share one host copy of each system-pinned model; and shared_host_weights_mode sets the permission bits (an octal string, default \"666\") for those shared regions and their lock files. The \"666\" default keeps cross-UID container setups working but is world-writable; \"660\" is recommended for production and multi-user systems.\n\n\n\n\n\n","title":"ReactantServerCore.RuntimeConfig"},{"category":"type","location":"api/config/#ReactantServerCore.SchedulerConfig","page":"Configuration","text":"SchedulerConfig\n\nGlobal scheduler settings (the scheduler: config block). ema_halflife_seconds is the half-life of the recent-compute moving average that drives fairness; recency_penalty_cap bounds the recency adjustment; coalescing_discount is the cost discount applied to coalesced batches; cost_ema_alpha is the smoothing factor for the learned per-batch-size cost; max_queue_depth caps each model's queue independently (a full model rejects new requests without affecting admission for the others); dispatch_timeout_seconds is the per-request execution timeout; discipline selects the inter-model ordering (see SchedulingDiscipline); and models holds the per-model ModelSchedConfig overrides.\n\n\n\n\n\n","title":"ReactantServerCore.SchedulerConfig"},{"category":"type","location":"api/config/#ReactantServerCore.ModelSchedConfig","page":"Configuration","text":"ModelSchedConfig\n\nPer-model scheduler overrides (an entry under scheduler.models). weight is the model's relative compute share (default 1.0, so all-default weights yield uniform shares; consulted only by the fair discipline). residency is the model's initial residency floor (see ResidencyState); nothing means unspecified, which the server resolves at startup to PINNED_SYSTEM when the on-demand weight cache is enabled (so every model's weights are materialized into host RAM and an on-demand GPU load is a pure host-to-device transfer) and UNPINNED otherwise. max_batch_size caps how many rows the scheduler coalesces into one dispatch of this model; nothing means uncapped. The cap does not change compiled shapes: a partial fill still pads up to the smallest compiled batch size, and a single request larger than the cap is still served (requests are never split).\n\n\n\n\n\n","title":"ReactantServerCore.ModelSchedConfig"},{"category":"type","location":"api/config/#ReactantServerCore.EndpointsConfig","page":"Configuration","text":"EndpointsConfig\n\nThe listen addresses (the endpoints: config block): host, the gRPC port, the optional metrics_port for the Prometheus exposition endpoint (0 = disabled), and max_concurrent_requests, the cap on simultaneously-handled RPCs (0 = uncapped). For a worker fronted by the gateway, bind host to all interfaces (0.0.0.0) so the gateway and Prometheus can reach it; the gRPC port is usually derived from the node file's base_port and the metrics port from metrics_base_port.\n\nmax_concurrent_requests is a worker-level overload backstop: past the cap, new requests are shed immediately with RESOURCE_EXHAUSTED rather than queued. Keep it strictly above the gateway's per-worker outbound stream limit so it never sheds traffic the gateway has already admitted (and so it never rejects a meta-model's loopback sub-call); in single-worker mode (no gateway, clients hit the worker directly) it is the only inbound admission control.\n\n\n\n\n\n","title":"ReactantServerCore.EndpointsConfig"},{"category":"type","location":"api/config/#ReactantServerCore.ModelControlMode","page":"Configuration","text":"ModelControlMode\n\nHow a worker manages the set of loaded models over its lifetime (mirrors NVIDIA Triton's model-control-mode). STATIC loads and compiles every bundle once at startup and never changes the set. DYNAMIC (the default) additionally runs a filesystem watcher that polls the model repository every model_poll_seconds and hot-swaps bundles as they are added, changed, or removed on disk. EXPLICIT cedes authority to an upstream control plane (the externally-managed residency behavior): no autonomous watcher, and non-resident models are not served until the control plane pins them.\n\n\n\n\n\n","title":"ReactantServerCore.ModelControlMode"},{"category":"type","location":"api/config/#ReactantServerCore.SchedulingDiscipline","page":"Configuration","text":"SchedulingDiscipline\n\nThe inter-model dispatch ordering. FAIR is the deficit-weighted, cost-aware policy with per-model weights and the coalescing discount; FIFO serves in global arrival order; EDF (earliest-deadline-first) serves the model whose most-urgent queued request has the soonest deadline. Coalescing runs underneath all three.\n\nGuidance: FAIR is for deployments where models share this worker with no upstream placement authority, a single-GPU worker or a multi-GPU fleet behind the round-robin gateway, where the worker itself must stop one model from crowding out the rest. Under the gateway's lpt_packing scheduling the gateway is the fairness authority (placement concentration plus the per-worker share cap), and workers must run FIFO or EDF so the two do not fight; lpt_packing supersedes the role FAIR played on manually-assigned multi-GPU fleets.\n\nEDF is for deadline-sensitive deployments where requests carry a remaining-budget timeout (see the request-level timeout parameter). It degrades to FIFO whenever queued requests share the same deadline, so its only divergence from FIFO is to promote requests with less budget left, in practice the in-flight meta-model sub-calls that have already spent part of their budget on an earlier stage. It also sheds work that cannot finish within its learned compute cost (laxity), so it trades some throughput (batch fragmentation, and no per-model fairness) for hitting more deadlines under load. NOTE: EDF derives urgency purely from the deadline, so issuing different per-client deadlines for the same model reorders that model's service and therefore affects fairness across clients; uniform deadlines keep it behaving like FIFO.\n\n\n\n\n\n","title":"ReactantServerCore.SchedulingDiscipline"},{"category":"type","location":"api/config/#ReactantServerCore.ResidencyMode","page":"Configuration","text":"ResidencyMode\n\nWho owns device residency on a worker, fixed at startup. SELF_MANAGED lets the worker autonomously transfer and evict weights above each model's floor within the device budget; EXTERNALLY_MANAGED makes a control plane authoritative (no autonomous eviction, non-resident models are not served until pinned). This is no longer configured directly: it is derived from ModelControlMode (explicit ⇒ EXTERNALLY_MANAGED, otherwise SELF_MANAGED).\n\n\n\n\n\n","title":"ReactantServerCore.ResidencyMode"},{"category":"type","location":"api/config/#ReactantServerCore.ResidencyState","page":"Configuration","text":"ResidencyState\n\nThe residency floor an operator (self-managed) or control plane (externally-managed) sets for a model's weights. UNPINNED keeps no guaranteed residency (loaded from the mmap on demand); PINNED_SYSTEM guarantees the weights stay resident in host RAM (and must be transferred to the device before execution); PINNED_DEVICE guarantees them resident on the GPU for the server's lifetime (exempt from eviction).\n\n\n\n\n\n","title":"ReactantServerCore.ResidencyState"},{"category":"method","location":"api/config/#ReactantServerCore.load_node-Tuple{AbstractString}","page":"Configuration","text":"load_node(path) -> Dict{String,Any}\n\nRead and structurally validate a node config file.\n\n\n\n\n\n","title":"ReactantServerCore.load_node"},{"category":"method","location":"api/config/#ReactantServerCore.load_node_raw-Tuple{AbstractString}","page":"Configuration","text":"load_node_raw(path) -> Dict{String,Any}\n\nRead a node config file without structural validation. Used by the supervisor, which may need to synthesize the workers: list (see materialize_node!) before validating.\n\n\n\n\n\n","title":"ReactantServerCore.load_node_raw"},{"category":"method","location":"api/config/#ReactantServerCore.materialize_node!-Tuple{AbstractDict, AbstractVector{<:AbstractString}}","page":"Configuration","text":"materialize_node!(node, devices; cpu_workers=1) -> Vector{Union{String,Nothing}}\n\nPrepare a raw node config for supervised single-container deployment: assign one visible device per worker and return the per-worker device selectors (in worker declaration order; nothing means the worker gets no CUDA_VISIBLE_DEVICES of its own, the CPU case).\n\nWith no workers: list, one is synthesized: worker0..workerN-1, one per device (or cpu_workers workers when devices is empty). An explicit workers: list wins: each worker is assigned devices[i] positionally, or devices[gpu+1] when the entry carries a gpu: key. The gpu: key is consumed here (deleted after assignment) so each child process, seeing a single device through CUDA_VISIBLE_DEVICES, resolves device ordinal 0. Assigning one device to two workers, or having more workers than devices, is a ConfigError. Call validate_node on the result before use.\n\n\n\n\n\n","title":"ReactantServerCore.materialize_node!"},{"category":"method","location":"api/config/#ReactantServerCore.node_gpus-Tuple{AbstractDict}","page":"Configuration","text":"node_gpus(node) -> :auto | Int | Vector{String}\n\nParse the optional top-level gpus: key: auto (the default when absent) asks the supervisor to enumerate visible devices; an integer is a device count (expanded to ordinals 0..n-1); a list gives explicit device selectors (ordinals or GPU UUIDs, passed to CUDA_VISIBLE_DEVICES verbatim).\n\n\n\n\n\n","title":"ReactantServerCore.node_gpus"},{"category":"method","location":"api/config/#ReactantServerCore.node_server_config-Tuple{AbstractDict, Union{Nothing, AbstractString}}","page":"Configuration","text":"node_server_config(node, worker) -> (ServerConfig, applied_overrides, worker_name)\n\nResolve the ServerConfig for one worker of a node. worker may be nothing when the node has exactly one worker (it defaults to that sole entry); otherwise it must name a defined worker. Environment overrides (INFERENCE_SERVER_*) are applied on top, as for any server config. Does not validate; call validate_config on the result.\n\n\n\n\n\n","title":"ReactantServerCore.node_server_config"},{"category":"method","location":"api/config/#ReactantServerCore.validate_node-Tuple{AbstractDict}","page":"Configuration","text":"validate_node(node)\n\nStructural validation of a parsed node config. Raises ConfigError on a malformed file: missing model_repo, duplicate worker names, colliding ports, or a models: entry that targets an undefined worker. The models: map is optional for any node: when omitted, every worker loads every bundle in the repo; when present, it is a per-model override that pins the named models to device memory on the listed workers (see worker_raw_config).\n\n\n\n\n\n","title":"ReactantServerCore.validate_node"},{"category":"method","location":"api/config/#ReactantServerCore.worker_names-Tuple{AbstractDict}","page":"Configuration","text":"worker_names(node) -> Vector{String}\n\nThe worker names in declaration order.\n\n\n\n\n\n","title":"ReactantServerCore.worker_names"},{"category":"method","location":"api/config/#ReactantServerCore.worker_raw_config-Tuple{AbstractDict, AbstractString}","page":"Configuration","text":"worker_raw_config(node, name) -> Dict{String,Any}\n\nResolve a single worker's raw config dict from a (validated) node config: deep-merge global with the named worker's override blocks, then set model_dirs to the shared repo, the endpoint port from base_port, the runtime device ordinal from the worker's GPU, and the node-level shared_host_weights flag. Every worker loads every bundle in the repo; the top-level models: map is an optional per-model override that pins the named models to device memory on the listed workers (translated here into scheduler.models.<name>.residency: device). The result has the shape build_config consumes.\n\n\n\n\n\n","title":"ReactantServerCore.worker_raw_config"},{"category":"section","location":"manual/docker/#Docker-Deployment","page":"Docker Deployment","text":"The repository ships a unified reactantserver image whose default entry point is the node supervisor (ReactantServerNode.supervise). It detects every GPU granted to the container, spawns one single-GPU worker subprocess per device, and multiplexes the children's logs onto the container's stdout with [worker0] / [gateway] line prefixes, restarting children that die. With two or more workers it also runs an embedded gateway on the public ports; with a single worker it skips the gateway entirely and binds that worker to the public ports directly, since a lone worker already serves the full KServe V2 API. Either way the external interface is the same. A multi-GPU deployment is therefore a single container with no per-GPU configuration:\n\ndocker run --gpus all --ipc=host -p 8001:8001 -p 8002:8002 \\\n  -v /path/to/bundles:/var/lib/reactantserver/models:ro reactantserver\n\nClients connect to the KServe V2 gRPC endpoint on localhost:8001; health and metrics are on localhost:8002 (/readyz, /healthz, /metrics). These match NVIDIA Triton's gRPC (8001) and metrics (8002) ports; the server is gRPC only, so Triton's HTTP port 8000 is unused. Adding a GPU to the host changes nothing in the configuration: the supervisor sees one more device and spawns one more worker.","title":"Docker Deployment"},{"category":"section","location":"manual/docker/#Files","page":"Docker Deployment","text":"docker/Dockerfile — the reactantserver image (julia:1.12.5-trixie). It copies the whole packages/ tree and builds the workspace root (the shared Manifest.toml pins the Reactant/HTTP forks). The default entry point is the supervisor; entrypoint.worker.sh stays in the image as the single-worker escape hatch.\ndocker/entrypoint.node.sh — the default entry point; launches the supervisor under julia --handle-signals=no (so its own SIGTERM/SIGINT handler runs) via tini.\ndocker/entrypoint.worker.sh — launches a single ReactantServer.serve worker named by REACTANT_WORKER_NAME; the single-worker escape hatch, not the default.\ndocker/healthcheck.node.sh — role-aware container healthcheck: curls the gateway's /readyz in the all/gateway roles (for a single worker, the worker's own /readyz), falls back to the Julia worker probe for workers.\ndocker/healthcheck.worker.jl — lightweight Julia readiness probe (imports only gRPCClient and YAML, never Reactant). With REACTANT_WORKER_NAME set it probes that worker; unset, it probes every worker in the node file and passes when at least one is ready.\ndocker/node.default.yaml — the zero-config node file baked into the image at /etc/reactantserver/node.yaml (gpus: auto, no workers: list).\ndocker/node.yaml — the fully commented node-file template to mount over the baked default (see Node Configuration).\ndocker/gateway.yml — optional config for the embedded gateway (scheduling knobs); the supervisor synthesizes the worker endpoints, so the gateway needs no file by default.\ndocker-compose.yml — the single-service stack (equivalent to the docker run above).","title":"Files"},{"category":"section","location":"manual/docker/#Prerequisites","page":"Docker Deployment","text":"Populate the vendored submodules:\ngit submodule update --init --recursive\nThis fetches lib/Reactant.jl, lib/gRPCServer.jl, lib/gRPCClient.jl, and lib/HTTP.jl.\nInstall the NVIDIA Container Toolkit on the host (for GPU access).\nHave a model bundle repository on the host. Each immediate subdirectory with a manifest.yaml is a bundle; its directory name is the model name (see Bundles & model.jl).","title":"Prerequisites"},{"category":"section","location":"manual/docker/#Build-and-run","page":"Docker Deployment","text":"make image        # or: docker compose build\nREACTANTSERVER_MODELS=/path/to/bundles docker compose up\n\nEvery model compiles (Reactant → device executable) before the gRPC plane accepts traffic, so first startup is slow and the image healthcheck's start_period is generous (300s); raise it for large model sets. Logs from all workers and the gateway appear on the container's stdout, one line each, prefixed [worker0], [worker1], …, [gateway], [supervisor].","title":"Build and run"},{"category":"section","location":"manual/docker/#Configuring","page":"Docker Deployment","text":"Zero configuration is the default: the baked node file sets gpus: auto and the supervisor synthesizes worker0..workerN-1, one per detected device. To customize, mount a node file over /etc/reactantserver/node.yaml (see Node Configuration for the full surface):\n\ngpus: — auto (default), an integer count, or an explicit device list (ordinals or GPU UUIDs). The REACTANT_GPUS environment variable overrides this key.\nworkers: — optional. When present it wins over auto-detection: device i goes to worker i (or a worker's gpu: key picks a specific visible device).\nmodels: — optional per-model pinning to device memory on named workers.\nglobal: — defaults merged into every worker (runtime, scheduler, cache_dir, endpoints).\n\nSupervisor environment variables: REACTANT_GPUS (count or device list; 0 for a CPU node), REACTANT_CPU_WORKERS (workers on a CPU node, default 1), REACTANT_WORKER_THREADS (compute threads per worker; default is the host's share, min(CPU_THREADS ÷ workers, 16), see Scaling to Multiple GPUs), REACTANT_ROLE (below), REACTANT_SUPERVISOR_MAX_RESTARTS (consecutive crash budget per child before the node exits 1; default unlimited, with the healthcheck reporting unready instead), REACTANT_NODE_FILE, and REACTANT_GATEWAY_FILE. The supervisor writes the materialized node file (with the synthesized workers: list) to /run/reactantserver/node.yaml for inspection; children and the healthcheck read that file.","title":"Configuring"},{"category":"section","location":"manual/docker/#Roles","page":"Docker Deployment","text":"REACTANT_ROLE selects what the supervisor runs; the default all (workers plus the embedded gateway, on one host) is what these guides cover. The workers and gateway roles exist to split a deployment across machines (GPU nodes behind a separate gateway host), but multi-node is not a shipped example here. See the Multi-GPU Gateway page for the gateway's behavior.","title":"Roles"},{"category":"section","location":"manual/docker/#Health-status","page":"Docker Deployment","text":"The container's healthcheck is healthcheck.node.sh, which dispatches on REACTANT_ROLE. In the all / gateway roles it curls /readyz on the metrics port (the embedded gateway's, or, for a single-worker node, the worker's own); both report ready once the process is up and at least one worker has reported ServerReady. In the workers role it runs the Julia probe against every worker in the materialized node file. \"At least one worker ready\" is the right liveness signal for a multi-GPU container: one failed GPU should not get a container serving the others killed. Because model compilation runs before the gRPC plane accepts traffic, start_period is generous (300s by default); raise it for large model sets.\n\nPer-worker readiness remains visible as the gateway_worker_ready{worker=\"...\"} Prometheus metric, and the supervisor logs each child's crash and restart. Compose health reflects process and serving readiness, not raw GPU hardware state, which the NVIDIA tooling exposes on the host.","title":"Health status"},{"category":"section","location":"manual/docker/#Metrics","page":"Docker Deployment","text":"One scrape on 8002 covers everything. With multiple workers, the embedded gateway's /metrics serves its own gateway_* series and fans out to every worker's metrics endpoint, merging the results into one exposition; with a single worker (no gateway), 8002 is that worker's own /metrics directly. Either way, each worker tags all of its series (worker_* plus process_*/julia_gc_*) with worker and gpu labels itself, where gpu is the physical device behind the worker's CUDA_VISIBLE_DEVICES, so no scrape-config relabeling is needed:\n\nscrape_configs:\n  - job_name: reactantserver\n    static_configs:\n      - targets: ['node:8002']\n\nPer-GPU and per-worker views fall out of the labels, e.g. sum by (gpu) (rate(worker_dispatch_total[1m])). Per-endpoint scrape health is reported as gateway_worker_metrics_up{endpoint=...}.","title":"Metrics"},{"category":"section","location":"manual/docker/#Single-GPU-soak-test","page":"Docker Deployment","text":"docker-compose.gpu2.yml runs the same supervised container on one GPU (CUDA_VISIBLE_DEVICES=2, so one worker and no gateway, serving 8001/8002 directly) plus a loadgen service that drives sustained concurrent requests, to exercise the full serving path and watch for memory leaks. It pins a persistent XLA compile-cache volume so warm restarts skip recompilation, and docker/monitor_gpu2.sh logs GPU memory and container RSS to a CSV for leak detection. Because it runs the production single-GPU container, the soak tests exactly what users deploy.","title":"Single-GPU soak test"},{"category":"section","location":"manual/docker/#Two-GPU-lpt_packing-soak-test","page":"Docker Deployment","text":"docker-compose.gpu23.yml is the multi-GPU counterpart: the same supervised container across two GPUs (CUDA_VISIBLE_DEVICES=2,3, so two workers behind the embedded gateway) with the gateway in lpt_packing mode. It mounts docker/node.gpu23.yaml, which sets scheduler.discipline: fifo (required by lpt_packing), and enables packing via REACTANT_GATEWAY_SCHEDULING_* environment. Use it to exercise the gateway's placement and coalescing-aware routing on the full serving path, not just a lone worker.\n\nThe loadgen report line includes the fleet weight-cache loads=/evicts= totals with per-window deltas (scraped from the aggregated /metrics). A steadily rising evicts means the model set does not fit resident and the workers are thrashing weights (host-to-device reloads, which cost CPU) — a different problem from CPU oversubscription, addressed by more weight_cache_bytes, pinning hot models, or more GPUs.","title":"Two-GPU lpt_packing soak test"},{"category":"section","location":"manual/docker/#Security","page":"Docker Deployment","text":"ReactantServer is designed to run on a trusted network behind your own perimeter. Be aware of the following before exposing any endpoint:\n\nAll gRPC traffic (worker and gateway) is cleartext h2c. TLS settings are parsed by the gateway config but not yet enforced; a configured cert triggers a startup warning.\nThere is no authentication or authorization on the KServe data plane, the worker control-plane RPCs (residency and policy), or the Prometheus metrics listener (which binds 0.0.0.0:8002 by default).\nModel bundles are trusted input: a bundle's optional model.jl executes arbitrary Julia in the server process. Only serve bundles you built or audited.\nPOSIX shared memory is a local trust boundary. Client-registered regions and the optional node-shared host-weight store live in /dev/shm; the shared weight regions default to mode 666 (world-writable) for cross-container friction-free sharing. Set runtime.shared_host_weights_mode: \"660\" on production or multi-user systems.\n\n","title":"Security"},{"category":"section","location":"design/architecture/#ReactantServer:-Project-Overview","page":"Architecture","text":"","title":"ReactantServer: Project Overview"},{"category":"section","location":"design/architecture/#What-it-is","page":"Architecture","text":"ReactantServer is a production inference server that serves many models compiled through Reactant.jl from a single Julia process on one GPU. Models are delivered as self-contained bundles (an MLIR module, its weights, and a manifest), compiled once through Reactant's PJRT bindings, and served over the KServe V2 inference API via gRPC, so standard Triton and KServe clients connect without changes. It targets static-graph workloads such as computer vision and scientific computing, where many models share a GPU and one model executes at a time.\n\nThe server is Reactant-centric, not XLA-specific: it runs whatever Reactant can lower to a device executable. Today that means StableHLO compiled by XLA — the only dialect and backend currently wired up, so in practice the focus is XLA — but the architecture is not bound to XLA, and any MLIR dialect Reactant exposes would serve the same way.","title":"What it is"},{"category":"section","location":"design/architecture/#Package-layout","page":"Architecture","text":"The project is a Julia workspace of five packages (plus the non-member ReactantServerExport for offline bundle export), split so that talking to a server never requires the heavy Reactant/XLA stack:\n\nReactantServerCore — the shared, Reactant-free substrate: the dtype vocabulary, the KServe V2 protobuf messages, the transport-agnostic boundary types, the manifest parser, server/node config, the wire codec, the shared-memory registry, and the concurrency-safe staging BufferPool. The other packages all build on it.\nReactantServer — the inference worker. It owns the model registry, the runtime, the scheduler, and the KServe V2 gRPC server, and is the only package that loads Reactant. Exports serve, serve_worker, stop!, register_model.\nReactantServerGateway — the multi-GPU reverse proxy (serve_gateway, probe_worker_ready). Builds only on Core and the gRPC layer; no Reactant.\nReactantServerClient — the inference client (KServeModel, infer_sync, infer_async, InferInput, InferOutput). Also Reactant-free, so it installs on a plain client machine. See Client Usage.\nReactantServerNode — the single-container node supervisor (supervise). It detects the visible GPUs and runs one worker subprocess per device, plus the embedded gateway when there is more than one worker. Reactant-free (it only orchestrates subprocesses).\n\nThe offline model-export tooling (packages/ReactantServerExport, with a PythonCall-triggered PyTorch extension) is not a workspace member; it produces bundles the worker consumes.","title":"Package layout"},{"category":"section","location":"design/architecture/#What-it-aims-to-achieve","page":"Architecture","text":"The project has one economic goal: serve the largest possible number of models per GPU at a given quality of service, while running each model as fast as the hardware allows. GPU memory is the dominant cost in inference infrastructure, so serving more models per card directly lowers cost per inference. Two technical levers deliver this:\n\nFit more models on a GPU. Weights do not all have to live on the GPU at once. The server keeps every model's weights resident in host RAM and moves a model's weights onto the GPU only when it is needed, evicting cold models to stay within a memory budget. This decouples the number of servable models from GPU memory capacity.\nRun each model faster with an ahead-of-time compiler. Each model is compiled once into an optimized executable (XLA today). The compiler performs whole-program optimization, fuses kernels across operations, and plans memory layout, which makes execution significantly faster than running the equivalent eager-mode graph. Small teams get compiler-grade performance without building it themselves.\n\nThe broader mission, the target audience, and the explicit non-goals are on the Philosophy page. Operational and format details are in the Getting Started and Node Configuration guides.","title":"What it aims to achieve"},{"category":"section","location":"design/architecture/#How-it-fits-together","page":"Architecture","text":"Bundles. A model is a directory with manifest.yaml, one or more StableHLO modules (model.b{N}.mlir, one per compiled batch size), a shared weights.safetensors, and an optional model.jl for pre/post-processing. Bundles are produced by offline conversion tooling from Lux or PyTorch models and loaded at server startup.\nOne process per GPU, one container per node. Each worker drives a single GPU, holds a single shared memory pool, and serves the full KServe V2 gRPC surface on its own. A node supervisor (ReactantServerNode) runs the whole node from one container: it detects the visible GPUs and starts one worker process per device. With a single worker it binds that worker to the public ports directly (no gateway); with two or more it also starts a thin embedded gateway that gives clients one endpoint and routes each model's traffic across workers, either uniformly (round robin) or by packing each model onto a fixed, operator-configured number of GPUs with coalescing-aware routing (lpt_packing). See the Scaling to Multiple GPUs and Multi-GPU Gateway guides.\nWeights as explicit arguments. Weights are passed to the compiled executable as arguments rather than baked into it. This is what makes on-demand loading and weight sharing across batch sizes possible: compilation is independent of where the weights currently live.","title":"How it fits together"},{"category":"section","location":"design/architecture/#Fitting-more-models-on-the-GPU","page":"Architecture","text":"Because only one model executes at a time, the GPU does not need every model's weights resident simultaneously. The server organizes weights in two tiers:\n\nHost RAM (resident by default). With the on-demand cache enabled, every model's weights default to system-pinned: materialized once from disk into host RAM at startup and kept there (residency: unpinned opts a model out). Host memory is plentiful and cheap relative to GPU memory, so this costs little and removes disk from the hot path.\nGPU (managed working set). Models marked as pinned keep their weights on the GPU for the server's lifetime. Every other model is loaded onto the GPU on demand when a request arrives, kept resident afterward so repeat requests are free, and evicted under a configured GPU byte budget using a least-recently-used policy. Eviction frees the device memory immediately through an explicit PJRT buffer release.\n\nBecause the weights are already in RAM, an on-demand GPU load is a single host-to-device transfer rather than a reload from disk. In practice this is tens of milliseconds even for the largest models, which is the same order of magnitude as a single inference. The effect is that a GPU sized for a handful of models can serve a much larger catalog, paying a small transfer cost only when a cold model is first called.","title":"Fitting more models on the GPU"},{"category":"section","location":"design/architecture/#Model-lifecycle","page":"Architecture","text":"How the set of loaded models changes over a worker's lifetime is set by model_control_mode (mirroring Triton's model control modes):\n\ndynamic (the default) watches the model repository and reconciles continuously: new bundles are loaded, removed bundles are unloaded, and changed bundles are reloaded. Change detection covers the weights, the MLIR, the manifest, and the bundle's model.jl, so updating a model's weights or its Julia pre/post-processing is just writing the files. A two-poll debounce keeps half-written bundles from loading, and a reload swaps one model atomically while every other model keeps serving.\nstatic loads the startup set once and never changes it.\nexplicit cedes the lifecycle to an external control plane: the worker takes no autonomous action, and model residency is driven entirely over a small gRPC control surface (ModelControlStatus to observe, SetModelResidency to pin and unpin, SetModelPolicy to adjust scheduling weight). A model serves only while the control plane holds it resident. This is the integration seam for organizations that run their own placement logic.","title":"Model lifecycle"},{"category":"section","location":"design/architecture/#The-scheduler","page":"Architecture","text":"The scheduler is the component that decides, each time the GPU is free, which model to run next and how many queued requests to serve in one execution. It coalesces queued requests and dispatches one model at a time under a configurable inter-model discipline, runs on top of the language task scheduler, and adds no threading of its own. Exactly one GPU execution runs at a time by design, which keeps memory and concurrency reasoning simple and is what makes the single-resident-model strategy above safe.\n\nTwo disciplines are supported. fair (the default) is a deficit-weighted, cost-aware share that balances GPU time across models on the worker. fifo ignores per-model weights and serves in global arrival order; it is the right choice when the worker sits behind a gateway running lpt_packing, which moves the placement decisions upstream (the two would otherwise fight). Coalescing applies under both.","title":"The scheduler"},{"category":"section","location":"design/architecture/#Structure","page":"Architecture","text":"Requests arrive concurrently over gRPC, each on its own task. A request's preprocess hook runs on that task before the request is queued, so it is queued already executable-ready; the request becomes visible to the dispatch loop only after preprocess has returned. A single dispatch loop then selects the next model, coalesces that model's queued requests into one execution at a compiled batch size, runs it, and hands each caller back its raw output slice; the caller's task runs the postprocess hook on that slice. One lock guards the queues and the per-model statistics and wakes the loop when work arrives. The loop holds the lock only to select and dequeue; the GPU execution runs outside the lock, so new requests keep arriving during a running inference.\n\nBecause the hooks run on the per-request tasks rather than on the dispatch loop, the CPU-side pre/post work of many requests proceeds in parallel and overlaps the single, serialized GPU execution: while the loop runs one model on the GPU, other requests are being pre/post-processed on other threads, and the loop runs no model.jl code itself. The GPU still executes exactly one model at a time (that is solely a property of the single loop). Realizing the overlap needs more than one OS thread; the supervisor starts each worker with a bounded share of the host (min(cores ÷ workers, 16) compute threads plus one interactive thread, overridable via REACTANT_WORKER_THREADS, so co-located workers do not oversubscribe the CPU) and the loop runs on the lone interactive thread so a blocking GPU call never starves it of CPU. (A bare ReactantServer.serve uses whatever --threads you pass.)","title":"Structure"},{"category":"section","location":"design/architecture/#Decision-order","page":"Architecture","text":"Each time the GPU frees up, the scheduler decides in this order:\n\nSelect the next model, according to the configured discipline. Under fair (the default), each model has a relative weight that defines its share of compute; the scheduler tracks a decaying exponential moving average of how much GPU time each model has recently consumed, and scores every model with a non-empty queue by how far below its share it is, divided by its estimated cost. A model that has used less than its share recently scores higher, and dividing by cost stops an expensive model from blocking cheaper ones on a marginal edge; a clamp bounds both lockout and domination. Under fifo, weights are ignored and the model with the oldest queued request wins (chosen behind an lpt_packing gateway, as above).\nCoalesce the winner. The selected model's queued requests are taken in FIFO order and packed into the largest compiled batch size that fits, padding a partial batch up to the smallest size when needed and always making forward progress on at least the oldest request. The remainder stays queued for the next round.\n\nPer-model latency budgets (earliest-deadline-first escalation ahead of the discipline) are a planned extension; they are not implemented today.","title":"Decision order"},{"category":"section","location":"design/architecture/#Cost-learning-and-coalescing","page":"Architecture","text":"The scheduler measures the real GPU time of each execution and refines a per-batch-size cost estimate with an exponential moving average, seeded by a warmup pass at startup so the first real requests are scheduled sensibly. Coalescing concatenates the inputs of several same-model requests along the batch axis into one execution, then slices the outputs back per caller. Only models whose inputs and outputs carry a batch axis are coalesced; others serve one request per dispatch.\n\nCoalescing is the throughput lever. Packing many requests into one execution amortizes the fixed per-launch overhead and, for a model that had to be loaded on demand, the one-time weight transfer, across every image in the batch. On a representative compute-heavy model, per-image latency drops by roughly three times from batch 1 to batch 32 while images per second more than triples.","title":"Cost learning and coalescing"},{"category":"section","location":"design/architecture/#Configuration-and-observability","page":"Architecture","text":"Per model, operators set a relative weight (used by the fair discipline), the residency floor, and whether the model is pinned to the GPU. Globally they set the discipline, the recent-compute half-life and a coalescing cost discount (both fair-only), the cost estimate smoothing, a per-model maximum queue depth, and the GPU weight-cache byte budget. Configuration is typed YAML with environment-variable overrides. The scheduler exposes per-model metrics: dispatch count, total compute, current recent-compute load, queue depth, queue-wait percentiles, and the histogram of coalesced batch sizes, plus weight cache residency and load/evict counters.","title":"Configuration and observability"},{"category":"section","location":"design/architecture/#The-compiler-advantage","page":"Architecture","text":"Serving static graphs through an ahead-of-time compiler is the project's second source of leverage. Reactant lowers each model to an MLIR module that is compiled once into a device executable; today that is StableHLO compiled by XLA. The compiler optimizes the whole program rather than one operation at a time: it fuses adjacent operations into single kernels, eliminates redundant work, and plans tensor layouts for the target device. The result executes substantially faster than the same model run eagerly, and the cost is paid once at startup rather than on every request. A bundle may carry several compiled batch sizes that share one set of weights, so the scheduler can pick the executable that matches each coalesced batch without duplicating parameters.","title":"The compiler advantage"},{"category":"section","location":"design/architecture/#Scope","page":"Architecture","text":"ReactantServer is opinionated and deliberately narrow. It is for small and mid-size engineering organizations that need efficient inference on static-graph models and that measure their own systems. It is Reactant-centric — currently focused on StableHLO/XLA — and is not a general multi-framework server, not an LLM serving stack, and not a managed service. The reasoning behind these boundaries is on the Philosophy page.\n\n","title":"Scope"},{"category":"section","location":"manual/meta_models/#Meta-Models","page":"Meta Models","text":"A meta model is a bundle whose model.jl orchestrates other models with ordinary Julia in between, rather than wrapping a single compiled executable. It exists for the logic torch.export cannot trace: data-dependent control flow, loops whose bounds depend on a tensor's contents, or a pipeline where stage two's inputs are computed from stage one's outputs. The server runs the orchestration as a normal inference request, so a meta model is addressable by clients under its own name exactly like any other model.\n\nIf your model is a single traced graph, use a plain bundle with optional pre/post hooks (see Bundles & model.jl). Reach for a meta model only when the glue between sub-models is real program logic.","title":"Meta Models"},{"category":"section","location":"manual/meta_models/#Anatomy","page":"Meta Models","text":"A meta bundle is a directory with a manifest.yaml and a model.jl, and no compiled artifact or weights of its own. The manifest declares kind: meta, lists the models the orchestration is allowed to call, and must carry client_inputs/client_outputs (there is no executable to infer the client-facing I/O from):\n\nformat_version: \"2.0\"\nname: object_detector\nkind: meta\nmeta:\n  calls: [object_detector_stage1, object_detector_stage2]\nclient_inputs:\n  - {name: IMAGE, dtype: f32, shape: chw, dims: {c: 3, h: 1024, w: 1024}}\nclient_outputs:\n  - {name: BOXES, dtype: f32, shape: nb, dims: {b: 4}}\n\nThe model.jl calls register_meta_model with the orchestration function:\n\nregister_meta_model(\"object_detector\"; run = function (inputs, call)\n    feats = call(\"object_detector_stage1\", inputs)            # backbone\n    rois  = compute_rois(feats)                              # ordinary Julia, data-dependent\n    out   = call(\"object_detector_stage2\", rois)              # head\n    return [ReactantServer.NamedTensor(\"BOXES\", out[1].data)]\nend)\n\nrun has the form run(inputs::Vector{NamedTensor}, call) -> Vector{NamedTensor}. The injected call(model_name, inputs) invokes another model and returns its outputs as NamedTensors. The callee must appear in meta.calls; calling an undeclared model is a loud error. The loader also rejects a meta bundle whose model.jl calls register_model instead of register_meta_model.","title":"Anatomy"},{"category":"section","location":"manual/meta_models/#Execution:-local,-on-the-request-task,-one-meta-at-a-time","page":"Meta Models","text":"A meta's run(inputs, call) executes on the gRPC request task, off the GPU dispatch loop, exactly like a model's pre/post hooks. Each call(name, inputs) re-enters the local scheduler in-process: the sub-model is dispatched on the loop and its result handed back to the orchestration. There is no gateway round-trip and no serialization between stages; a tensor handed from one stage to the next is passed by reference. The same model.jl runs unchanged on a single worker or in a fleet; the author never writes routing and never needs to know where anything is placed, because a meta only ever runs where its sub-models already live (see Placement, below).\n\nTwo rules shape how a meta shares the GPU with everything else on the worker:\n\nA bounded number of GPU metas at a time. A per-worker gate admits REACTANT_META_CONCURRENCY meta orchestrations at once, default 2. A meta holds its permit across the whole run, including the CPU glue between stages, but it does not hold the GPU during that glue. While a meta computes between stages, the dispatch loop is free and serves other models, so the GPU is not idle during the glue. The default of 2 lets two heavy pipelines overlap (one's glue hides behind another's GPU stage); at 1 two heavy metas serialize, while one holds the gate the other waits and can miss its deadline. Raising it further favors meta completion via more concurrency, but more metas share the serial GPU and the scratch pool (size it for that many concurrent metas). A compute-only meta (empty calls, below) issues no sub-calls and bypasses the gate entirely, so a heavy pure-Julia meta never holds a permit a GPU meta needs.\nIn-flight sub-calls jump the line. Once a meta holds the gate, each of its GPU stages is dispatched ahead of the queued regular work rather than waiting behind it. The number that can cut the line is tied to the gate: each in-flight meta contributes at most one pending sub-call, so the committed set is bounded by REACTANT_META_CONCURRENCY. At the default of 1 exactly one meta cuts the line and the behavior is fully deterministic; raising the gate lets that many metas cut the line symmetrically (no contention for a single privileged slot), so the one knob sets both how many metas run and how many are prioritized, a single dial for how strongly to favor meta completion.\n\nflowchart TD\n    C[Client] -->|\"infer(object_detector)\"| GW[Gateway]\n    GW -->|\"routes the whole group to the<br/>one worker that holds it\"| RT[\"Worker request task — run(inputs, call)<br/>holds a meta-gate permit (up to REACTANT_META_CONCURRENCY, default 2)\"]\n    RT -->|\"call('stage1', image)<br/>committed: jumps the queue\"| L((Dispatch loop / GPU))\n    L -.feature maps.-> RT\n    RT -->|\"Julia glue (GPU free for other models),<br/>then call('stage2', roi) by reference\"| L\n    L -.detections.-> RT\n    RT -->|assembled outputs| GW\n    GW --> C\n    OQ[\"Other models' requests\"] -.dispatched during the glue.-> L\n\n    classDef meta fill:#fff9e6,stroke:#cc9900,stroke-width:2px,color:#000\n    classDef gpu fill:#e6f3ff,stroke:#0066cc,stroke-width:2px,color:#000\n    class RT meta\n    class L gpu","title":"Execution: local, on the request task, one meta at a time"},{"category":"section","location":"manual/meta_models/#How-it-interacts-with-batch-scheduling","page":"Meta Models","text":"A meta no longer holds the GPU for its whole run. Each stage is an ordinary dispatch on the loop, so the GPU is occupied only while a stage actually computes and is free during the meta's CPU glue, when the loop serves other models. The trade is the gate: while a meta holds its permit, a second meta on the same worker waits (its stages cannot start), though the GPU is not wasted because regular models run in the meantime. A stage runs at batch 1 for the meta unless other clients' calls to the same sub-model happen to coalesce with it on the loop.\n\nA meta carries a deadline like any request. It is dropped before it starts if the deadline has already passed or if it cannot take a gate permit in time, so a meta backlog sheds at admission rather than piling up, and no GPU is spent on a meta that will not run. Once a meta is in flight, its continuation sub-calls are committed: they skip the predictive laxity drop, so a stage is never shed merely because it might run long after earlier stages already consumed GPU. They still honor the base check, so a meta whose deadline genuinely elapses mid-pipeline stops at the next stage boundary (raising DeadlineExceeded) instead of running the rest. The net effect is that a meta either runs to completion or is shed at a clean boundary, never silently discarding a stage it already paid for. Under the edf discipline the committed sub-call inherits the meta's deadline, so it is ordered ahead of fresher work (see the discipline notes in Node Configuration).","title":"How it interacts with batch scheduling"},{"category":"section","location":"manual/meta_models/#Placement:-the-group-travels-together","page":"Meta Models","text":"A meta owns no weights or executable of its own, so it is not placed on a GPU by itself. Instead the meta and the sub-models it calls form a group that the gateway places as a single unit. The worker reports the meta to the gateway with a memory footprint equal to the sum of its sub-models' weights and hides the sub-models from discovery, so the gateway packs and routes the group by the meta's own traffic and never sees the individual stages. When the group is placed on a GPU, the sub-models' weights are kept resident there together, so the meta's in-process sub-calls always find their executables and weights local.\n\nThis is the inverse of routing each sub-call independently. The author still writes the orchestration with no knowledge of placement, but the cost of that abstraction is now paid once, at placement time (the group is co-located), rather than per sub-call at request time (a hop to wherever each stage happened to land). The sub-models are internal to the meta: they are never addressable by clients and never appear in the gateway's routing table.","title":"Placement: the group travels together"},{"category":"section","location":"manual/meta_models/#Reuse-buffers-with-call.scratch","page":"Meta Models","text":"A meta's data-dependent glue often builds a large intermediate, for example a ~50 MB ROI feature tensor handed from one stage to the next. Allocating that fresh on every request drives GC pressure. The injected call exposes a reuse-buffer allocator for this case:\n\nroi = call.scratch((7, 7, 256, k), Float32)   # from the worker's reuse pool (or a plain array)\nfill_rois!(roi, feats, boxes)                  # write directly into it\nout = call(\"object_detector_stage2\", [ReactantServer.NamedTensor(\"ROI_FEATS\", roi)])\n\nThe pool is plain memory, local to the worker, and never shared across processes. It exists purely to keep large intermediates off the per-request allocation path; because the sub-call runs in-process, the buffer is handed to the next stage by reference, with no copy. A meta that never calls scratch just allocates normally, and the same model.jl is correct with or without a pool configured.\n\nRequest every buffer in one call.scratch call (pass a vector of dims => T pairs to get several at once); calling it more than once per request is rejected, and a scratch buffer must reach the sub-call as a contiguous array (a reshape or contiguous prefix is fine). Concurrent metas (up to REACTANT_META_CONCURRENCY gated metas, plus any compute-only metas) share the pool, so size it for that many in-flight metas; the deadline-bounded acquire degrades gracefully if it is ever starved.","title":"Reuse buffers with call.scratch"},{"category":"section","location":"manual/meta_models/#Constraints","page":"Meta Models","text":"A meta model may not call another meta model. This is validated at load: a group is exactly one level deep.\nCompute-only metas are allowed. A meta may declare an empty meta.calls and do all its work in Julia, calling no sub-models at all. This is useful for logic that is awkward to express as a traced graph but needs no separate executable. A compute-only meta issues no GPU sub-calls, so it bypasses the meta gate and runs on its request task without taking a permit.\nSub-models are internal. A model named in some meta's meta.calls is reachable only through that meta, never addressed by clients directly, and is hidden from the gateway's discovery and placement; the meta's group carries it.","title":"Constraints"},{"category":"section","location":"manual/meta_models/#See-also","page":"Meta Models","text":"Object Detection for a worked end-to-end meta model: converting a torchvision Faster R-CNN into two StableHLO stages chained by data-dependent Julia glue\nBundles & model.jl for the plain (non-meta) bundle path and pre/post hooks\nMulti-GPU Gateway for how the gateway routes and places models\nNode Configuration for the scheduling disciplines, including edf\nregister_meta_model in the API reference\n\n","title":"See also"},{"category":"section","location":"api/transport/#Transport","page":"Transport","text":"The wire layer beneath the boundary types: the KServe V2 codec that translates protobuf messages to and from InferRequest / NamedTensor, and the system shared-memory registry that backs the Triton-compatible zero-copy data plane. These live in ReactantServerCore (shared by the worker and the gateway) and are documented here for contributors.","title":"Transport"},{"category":"section","location":"api/transport/#Codec","page":"Transport","text":"","title":"Codec"},{"category":"section","location":"api/transport/#Shared-memory","page":"Transport","text":"","title":"Shared memory"},{"category":"section","location":"api/transport/#Staging-buffer-pool","page":"Transport","text":"The concurrency-safe staging pool the client drives inference through (and the precompile workloads exercise). A fixed-slot allocator over one contiguous backing region; a request may span several physically contiguous slots.\n\n","title":"Staging buffer pool"},{"category":"constant","location":"api/transport/#ReactantServerCore.TIMEOUT_NS_PARAM","page":"Transport","text":"TIMEOUT_NS_PARAM\n\nKey of the request-level KV parameter carrying the caller's REMAINING budget in nanoseconds (relative, not an absolute timestamp). Like the shared-memory region parameters, this is an extension to KServe V2 passed through ModelInferRequest.parameters. It is relative so each hop converts it to its own local absolute deadline (time_ns() + budget), which makes it robust to cross-process monotonic-clock differences and lets it ride unchanged through the gateway's raw-byte request forwarding. See deadline_params.\n\n\n\n\n\n","title":"ReactantServerCore.TIMEOUT_NS_PARAM"},{"category":"method","location":"api/transport/#ReactantServerCore.deadline_params-Tuple{Integer}","page":"Transport","text":"deadline_params(budget_ns) -> Dict{String,InferParameter}\n\nBuild the request-level parameters map carrying a remaining-budget timeout of budget_ns nanoseconds (see TIMEOUT_NS_PARAM). A non-positive budget_ns yields an empty map (no deadline). Merge the result into a ModelInferRequest's parameters.\n\n\n\n\n\n","title":"ReactantServerCore.deadline_params"},{"category":"function","location":"api/transport/#ReactantServerCore.decode_infer_request","page":"Transport","text":"decode_infer_request(msg, registry=nothing) -> DecodedRequest\n\nTranslate a decoded ModelInferRequest message into the boundary InferRequest. The transport (gRPC) hands us the already-decoded protobuf message, so the codec never touches wire bytes. Input tensor data is read from a registered shared-memory region (preferred when the tensor declares one), otherwise from rawinputcontents, otherwise from the typed contents field.\n\n\n\n\n\n","title":"ReactantServerCore.decode_infer_request"},{"category":"method","location":"api/transport/#ReactantServerCore.decode_infer_response-Tuple{ReactantServerCore.inference.ModelInferResponse}","page":"Transport","text":"decode_infer_response(msg) -> Vector{NamedTensor}\n\nTranslate a ModelInferResponse into boundary NamedTensor outputs. Data is read from rawoutputcontents when present, otherwise from the typed contents field. Shared-memory-backed outputs are not supported on this path (the caller never requests them).\n\n\n\n\n\n","title":"ReactantServerCore.decode_infer_response"},{"category":"method","location":"api/transport/#ReactantServerCore.encode_infer_request-Tuple{AbstractString, Vector{NamedTensor}}","page":"Transport","text":"encode_infer_request(model_name, inputs; requested_outputs=String[], id=\"\") -> ModelInferRequest\n\nBuild a ModelInferRequest from boundary NamedTensor inputs, with tensor data inline in rawinputcontents. requested_outputs, when non-empty, names the outputs to return.\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_request"},{"category":"method","location":"api/transport/#ReactantServerCore.encode_infer_request_shm-Tuple{AbstractString, Vector{NamedTensor}, AbstractString, Vector{<:Integer}}","page":"Transport","text":"encode_infer_request_shm(model_name, inputs, region, offsets; requested_outputs, id)\n\nEncode a request whose inputs are ALL staged in shared-memory region at the given byte offsets (parallel to inputs); no raw_input_contents — the receiver reads each tensor via shm_read. The receiver must have region registered. This is all-or-nothing per request: the decode path treats raw_input_contents as parallel-to-inputs, so a request never mixes raw and SHM inputs.\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_request_shm"},{"category":"method","location":"api/transport/#ReactantServerCore.encode_infer_response-Tuple{AbstractString, AbstractString, Vector{NamedTensor}}","page":"Transport","text":"encode_infer_response(model_name, id, outputs) -> ModelInferResponse\n\nBuild the response message with outputs entirely inline (rawoutputcontents). The transport serializes the returned message.\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_response"},{"category":"method","location":"api/transport/#ReactantServerCore.encode_infer_response-Tuple{AbstractString, DecodedRequest, Vector{NamedTensor}, Union{Nothing, SharedMemoryRegistry}}","page":"Transport","text":"encode_infer_response(model_name, decoded, outputs, registry) -> ModelInferResponse\n\nBuild the response message, writing any output whose requested entry named a shared-memory region into that region (and referencing it in the response) instead of inline. rawoutputcontents holds the inline outputs in order.\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_response"},{"category":"method","location":"api/transport/#ReactantServerCore.encode_model_metadata-Tuple{AbstractString, Manifest, AbstractString}","page":"Transport","text":"encode_model_metadata(name, manifest, platform) -> ModelMetadataResponse\n\nBuild a ModelMetadataResponse message from the manifest's client-facing I/O spec.\n\n\n\n\n\n","title":"ReactantServerCore.encode_model_metadata"},{"category":"method","location":"api/transport/#ReactantServerCore.encode_repository_index-Tuple{AbstractVector{<:AbstractString}}","page":"Transport","text":"encode_repository_index(names) -> RepositoryIndexResponse\nencode_repository_index(entries::AbstractVector{<:Pair}) -> RepositoryIndexResponse\n\nBuild a RepositoryIndexResponse. The first form lists every model as READY (direct-client introspection). The second takes name => ready::Bool pairs and reports READY or UNAVAILABLE per model, so the gateway can discover which replicas actually serve a model (readiness reflects residency on the worker).\n\n\n\n\n\n","title":"ReactantServerCore.encode_repository_index"},{"category":"method","location":"api/transport/#ReactantServerCore.shm_read-Tuple{SharedMemoryRegistry, AbstractString, Integer, Integer}","page":"Transport","text":"shm_read(registry, name, offset, byte_size) -> Vector{UInt8}\n\nCopy [offset, offset+byte_size) of the named region into a fresh byte vector. The copy is done while the region's lock is held, so a concurrent shm_unregister! of the same region cannot detach the mapping underneath the read.\n\n\n\n\n\n","title":"ReactantServerCore.shm_read"},{"category":"method","location":"api/transport/#ReactantServerCore.shm_register!-Tuple{SharedMemoryRegistry, AbstractString, AbstractString, Integer, Integer}","page":"Transport","text":"shm_register!(registry, name, key, offset, byte_size)\n\nAttach the existing POSIX shared-memory object key read-write and register it under name. Re-registering a name replaces (and detaches) the previous mapping.\n\n\n\n\n\n","title":"ReactantServerCore.shm_register!"},{"category":"method","location":"api/transport/#ReactantServerCore.shm_unregister!-Tuple{SharedMemoryRegistry, AbstractString}","page":"Transport","text":"shm_unregister!(registry, name)\n\nUnregister and detach a region, or all regions when name is empty.\n\n\n\n\n\n","title":"ReactantServerCore.shm_unregister!"},{"category":"method","location":"api/transport/#ReactantServerCore.shm_write!-Tuple{SharedMemoryRegistry, AbstractString, Integer, Vector{UInt8}}","page":"Transport","text":"shm_write!(registry, name, offset, bytes)\n\nCopy bytes into [offset, offset+length(bytes)) of the named region. The copy is done while the region's lock is held, so a concurrent shm_unregister! of the same region cannot detach the mapping underneath the write.\n\n\n\n\n\n","title":"ReactantServerCore.shm_write!"},{"category":"method","location":"api/transport/#ReactantServerCore.BufferPool-Tuple{Integer}","page":"Transport","text":"BufferPool(n_bytes; n_slots=8, use_shm=true, name=\"reactant_server_pool\")\n\nAllocate a staging pool of n_bytes divided into n_slots equal slots. slot_bytes is fixed at construction (n_bytes ÷ n_slots), not recomputed per request, so the allocator can hand disjoint slots to concurrent callers. A SHM-backed pool can be registered with a server; an inline pool (use_shm=false) is the fallback transport.\n\n\n\n\n\n","title":"ReactantServerCore.BufferPool"},{"category":"type","location":"api/transport/#ReactantServerCore.PoolAcquireTimeout","page":"Transport","text":"PoolAcquireTimeout(span, waited_ns)\n\nRaised by acquire_slot! when a deadline_ns was supplied and passed before span contiguous slots became free. The waiter is dequeued before this is thrown, so it never stalls the line. Callers that carry a request deadline (e.g. a meta model's fan-out) translate this into their own deadline-exceeded error.\n\n\n\n\n\n","title":"ReactantServerCore.PoolAcquireTimeout"},{"category":"function","location":"api/transport/#ReactantServerCore.acquire_slot!","page":"Transport","text":"acquire_slot!(pool, span=1; deadline_ns=0) -> PoolSlot\n\nBlock until span physically contiguous slots are free, then return them as one slot whose [offset, offset + span*slot_bytes) range no other in-flight slot overlaps. Throws an ArgumentError immediately if span exceeds the pool's total slot count, since such a request could never be satisfied and would otherwise deadlock. Waiters are served in FIFO order. Pair with release_slot!, which frees the whole run.\n\ndeadline_ns is an absolute time_ns() deadline (0 = wait indefinitely, the default and prior behavior). When set, a waiter that has not acquired by the deadline throws [PoolAcquireTimeout] instead of parking past it. A one-shot timer wakes the waiter at the deadline even if no slot is released in the meantime, so a starved waiter fails fast rather than burning a request's whole budget in the park.\n\n\n\n\n\n","title":"ReactantServerCore.acquire_slot!"},{"category":"method","location":"api/transport/#ReactantServerCore.release_slot!-Tuple{PoolSlot}","page":"Transport","text":"release_slot!(slot)\n\nReturn a slot acquired with acquire_slot! to the pool's free set, freeing every physical slot in its span. Releasing a derived subslot (index == 0) is a no-op. Throws if any slot in the span is already free (double release).\n\n\n\n\n\n","title":"ReactantServerCore.release_slot!"},{"category":"section","location":"manual/multi_gpu_gateway/#Multi-GPU-Gateway","page":"Multi-GPU Gateway","text":"The gateway is a gRPC reverse proxy that fronts several ReactantServer.jl workers behind one KServe V2 gRPC endpoint. It is pure Julia, lives in its own package ReactantServerGateway (ReactantServerGateway.serve_gateway), and reuses ReactantServerCore's node/config parsing and the generated KServe protobuf. Because it builds only on ReactantServerCore and the gRPC layer, the gateway carries no Reactant dependency.\n\nYou do not start the gateway yourself. When a node has two or more workers, the supervisor (ReactantServerNode.supervise, the container's default entry point) runs the gateway as an embedded child and synthesizes its worker endpoint list from the node file; a single-worker node skips it entirely (one worker already serves the full KServe V2 API). This page describes what that embedded gateway does. See Scaling to Multiple GPUs for when it appears and Docker Deployment for the container.\n\nClients connect to a single gRPC endpoint. The gateway extracts the model name from each ModelInferRequest and forwards the raw protobuf bytes over gRPC to the worker that hosts that model. The KServe V2 protobuf wire format is identical end to end; the gateway is a gRPC-to-gRPC pass-through that never re-marshals the body.","title":"Multi-GPU Gateway"},{"category":"section","location":"manual/multi_gpu_gateway/#What-the-gateway-does","page":"Multi-GPU Gateway","text":"Single endpoint: clients reach all workers through one gRPC listener.\nModel-name routing by autodiscovery: the gateway is given a flat list of worker endpoints: in its own gateway.yml and queries each worker's RepositoryIndex RPC (every 10s) to learn which models it currently serves. The discovered model-to-workers routing table is rebuilt and swapped in atomically on each probe, so a control-plane pin/unpin or a worker restart flips routing on the next probe.\nReplica scheduling: a model served by more than one worker is load-balanced across those workers, either uniformly (round_robin, the default) or by packing each model onto a fixed, operator-configured number of GPUs with coalescing-aware routing (lpt_packing); see Scheduling modes below. Either way, a request fails over to the remaining replicas when a worker returns NotFound or Unavailable.\nReadiness probe: a background loop calls each worker's KServe ServerReady RPC; /readyz is ready when at least one worker reports ready.\nRaw passthrough: the ModelInfer hot path never decodes or re-marshals the protobuf body. The request and response types are Vector{UInt8} end to end (gRPCServer.jl and gRPCClient.jl support raw byte messages natively). To route, the gateway decodes a partial schema that declares only model_name (field 1) and id (field 3); ProtoBuf skips the tensor payload.\nSHM broadcast: SystemSharedMemoryRegister / Unregister are fanned out to every worker. POSIX SHM regions are host-local; every worker attaches via shm_open independently. Register succeeds only if all workers succeed (it rolls back partial success); unregister succeeds if any worker does.\nObservability: structured logs, Prometheus metrics, /healthz, and /readyz on a separate admin HTTP port.","title":"What the gateway does"},{"category":"section","location":"manual/multi_gpu_gateway/#Scheduling-modes","page":"Multi-GPU Gateway","text":"For guidance on choosing round_robin versus lpt_packing and setting replica counts for your situation, with an example configuration for each shape, see Common Use Cases.\n\nThe gateway routes each model's requests across its replicas according to scheduling.mode in gateway.yml:\n\nscheduling:\n  mode: lpt_packing             # round_robin (default) | least_outstanding | lpt_packing\n  rebalance_compute_seconds: 30 # fleet GPU-seconds consumed that triggers a repack\n  min_rebalance_seconds: 0      # wall-clock floor between repacks (0 = none)\n  rate_halflife_seconds: 30\n  hysteresis: 0.1               # minimum improvement before a model moves workers\n  default_replicas: 1           # GPUs per model unless overridden below (a number, or \"all\")\n  routing_fill_factor: 1.0      # per-replica fill target as a multiple of max batch size (lpt_packing only)\n  routing_policy: fill_rr       # fill_rr (default) | fill_least  (lpt_packing only)\n  models:\n    big-model:\n      replicas: 2               # this model is placed on 2 distinct GPUs (a number, or \"all\")\n\nround_robin (the default) spreads each model's requests uniformly across its replicas. It is fully predictable from the config file and needs no measurements, at the cost of thin per-worker queues: when every model is on every worker, each worker sees a slice of every model's traffic, so coalesced batches rarely fill.\n\nleast_outstanding sends each request to the replica with the fewest in-flight requests, spreading by live occupancy rather than blindly. Like round_robin it needs no measurements and no preconditions and does not concentrate traffic, so it favors even spreading over batch coalescing; prefer it over round_robin when a model's replicas have uneven or unpredictable per-request latency, so a slow replica stops attracting new work instead of accumulating a backlog.\n\nlpt_packing places each model on a fixed number of distinct GPUs and routes its requests to preserve batch fill. A model's replica count is operator-controlled: default_replicas (default 1, the single-GPU case that coalesces best), overridable per model under scheduling.models.<name>.replicas. Both accept a positive integer or all, which places the model on every ready worker (so default_replicas: all replicates the whole model set across all GPUs without listing each model, and tracks the fleet as workers come and go). The count is set at startup and never grows automatically under load; a hot model relies on its worker's queue and coalescing rather than fanning out.\n\nwarning: Replication is the operator's responsibility\nThe gateway does not check that a replica count is feasible for your hardware. Replicating a model charges its full weight footprint to every GPU it lands on, so replicas: 2 (or default_replicas: all) only makes sense when those weights actually fit on each card alongside everything else placed there. If the assigned footprint exceeds a worker's on-demand weight budget, the weights cannot all stay resident and the worker thrashes, loading and evicting weights on nearly every request, which destroys throughput. Size replica counts against your GPU memory. The gateway logs a weight footprint exceeds the worker's on-demand budget warning at each repack when a placement is oversubscribed, so watch for it. The\n\npacker chooses which GPUs host each model's replicas by balancing two live measurements: compute demand (the gateway-measured arrival rate times the true per-request compute cost the workers report over the control plane) and resident weight footprint against each worker's weight-memory budget, placing models heaviest-first onto the least pressured workers, where pressure is whichever of compute or memory is closer to full. Packing by memory keeps each GPU's resident weight set bounded so evictions stay rare. Placements are sticky: a single-replica model moves only when the move improves its resulting pressure by more than hysteresis, because batching depends on traffic staying where the queues are. (max_worker_share is accepted but advisory only; load no longer determines a model's GPU count.)\n\nRepacks are driven by accumulated compute, not wall-clock: the gateway polls the workers every probe round and recomputes the placement once the fleet has consumed rebalance_compute_seconds GPU-seconds since the last repack, subject to the min_rebalance_seconds wall-clock floor. An idle fleet does not repack until traffic resumes.\n\nFor a model with more than one replica, the gateway routes to fill one replica's batch before moving to the next, so the workers receive favorable groupings to coalesce (the coalescing itself stays at the worker). It tracks the in-flight request count per replica and keeps sending a model's requests to the replica it is currently filling until that replica holds about routing_fill_factor times the model's max batch size, then opens a fresh batch on another replica. Set routing_fill_factor above 1.0 to keep the next batch queued so a worker does not go idle between dispatches.\n\nrouting_policy (lpt_packing only) decides only which replica a fresh batch opens on (both variants preserve the fill-one-replica-first behavior above; they differ only at the batch boundary):\n\nfill_rr (default) round-robins the opening replica across the model's set, so successive batches of the same model spread evenly over its GPUs.\nfill_least opens each batch on the replica whose GPU currently carries the least in-flight compute load, measured across all models as in-flight requests weighted by each model's measured per-request compute cost. Prefer this when replicas share GPUs with other models, so a model's batches open on whichever of its GPUs is least busy rather than always the same one.\n\nSpreading every request without concentrating it is the separate least_outstanding scheduling mode above, not a routing policy.\n\nA single-replica model is the degenerate case: all its requests go to its one GPU (and still count toward that GPU's load for the fill_least decisions of models that share it).\n\nlpt_packing has two preconditions, verified at gateway startup: every worker must run the fifo scheduler discipline (placement decisions move to the gateway, so workers should not re-order against it; see scheduler.discipline in Node Configuration), and every worker must serve the identical model set. Because a worker compiles and warms up every model before its control plane answers, the workers are usually not up when the gateway starts, so the gateway waits for all of them before serving rather than failing, logging which workers are still pending. Under the node supervisor (the embedded gateway) this wait is enabled automatically; for a standalone gateway set REACTANT_GATEWAY_STARTUP_WAIT_SECONDS (a number of seconds, or inf to wait indefinitely; the default 0 fails fast). Each poll is watchdog-bounded, and if the gRPC client stack wedges during the long warmup (a known libcurl failure mode) the gateway exits so the supervisor restarts it with a fresh stack; this self-heals and you may see one such restart before it serves. Once all workers are up, a wrong discipline or differing model set is a hard error. Runtime drift after startup degrades gracefully: a model temporarily missing from some workers is routed uniformly over its actual replicas with a warning until the fleet converges, and a worker that drops out is excluded from placement, its traffic failing over to the remaining replicas.\n\nThe placement is observable: gateway_model_replicas reports each model's replica count, gateway_placement_weight reports its per-worker weight, gateway_replica_outstanding reports the in-flight requests per replica sampled at the last repack, and gateway_model_utilization reports its estimated demand in GPU-seconds per second.","title":"Scheduling modes"},{"category":"section","location":"manual/multi_gpu_gateway/#What-the-gateway-does-not-do","page":"Multi-GPU Gateway","text":"Streaming RPCs.\nThe repository / model-config / statistics / trace / log RPCs in the Triton spec, plus ServerLive, ServerReady, ModelMetadata, and RepositoryIndex for clients (only ModelInfer and the two SHM RPCs are proxied; everything else returns UNIMPLEMENTED).\nTLS: parsed but not yet enforced; the listener and the worker back-hop are cleartext h2c.\nCUDA shared memory.\nDynamic worker membership: the worker endpoint list is fixed at startup (from gateway.yml or REACTANT_GATEWAY_WORKERS). Which models each worker serves is rediscovered continuously, but adding or removing workers requires a gateway restart.","title":"What the gateway does not do"},{"category":"section","location":"manual/multi_gpu_gateway/#Configuration","page":"Multi-GPU Gateway","text":"The supervisor configures the embedded gateway for you: it synthesizes the worker endpoint list (and the worker metrics list) from the node file into REACTANT_GATEWAY_WORKERS / REACTANT_GATEWAY_WORKER_METRICS, and binds the gateway to the public ports (8001/8002). Nothing about model placement is configured on the gateway; it autodiscovers which models each worker serves via RepositoryIndex and refreshes its routing table periodically.\n\nTo tune the gateway, mount a docker/gateway.yml over /etc/reactantserver/gateway.yml (or set REACTANT_GATEWAY_FILE); it carries the gateway's own settings (listen addresses, message limits, logging, and the scheduling: block above). Settings can also be overridden by environment with the prefix REACTANT_GATEWAY_ and the dotted path uppercased with underscores, e.g. REACTANT_GATEWAY_LOGGING_LEVEL=debug or REACTANT_GATEWAY_SCHEDULING_MODE=lpt_packing.","title":"Configuration"},{"category":"section","location":"manual/multi_gpu_gateway/#Operational-notes","page":"Multi-GPU Gateway","text":"The gateway is a single point of failure. Each Julia worker stays reachable on its own KServe V2 gRPC endpoint during a gateway outage, so a client can fall back to addressing a worker directly.\nThe routing table is rebuilt every 10s from each worker's RepositoryIndex and swapped in atomically. If a worker dies, its routes persist until the next successful probe (up to ~10s); in the gap, requests to its models fail over to the remaining replicas (on NotFound or Unavailable), and a model with no live replica returns NotFound. The worker-side readiness probe (ServerReady, same 10s loop) drives /readyz and the gateway_worker_ready metric.\nUnder lpt_packing, the gateway polls the workers on every 10s probe round to refresh routing metadata and accumulate consumed compute, but recomputes the placement only once the fleet has consumed scheduling.rebalance_compute_seconds GPU-seconds (subject to the scheduling.min_rebalance_seconds floor). Each repack logs a lpt_packing: repack line with the number of models placed, how many moved workers, how many were held_by_hysteresis, the largest available max_improvement against the hysteresis threshold, and the compute_seconds/ wall_seconds since the last repack — useful for watching placement churn and the trigger cadence.\nIf a probe to a worker hangs (times out, rather than failing fast), the gateway drops and recreates that worker's gRPC connection before the next attempt. This recovers from a half-open connection (e.g. caught during a worker's brief silent-accept window at startup) that would otherwise be reused and stall every later request to that worker — the per-worker equivalent of a restart, without dropping HTTP/2 multiplexing for healthy workers.\nSuccessful ModelInfer requests are not logged (to keep the hot path quiet); worker errors and a model with no live replica are logged, and per-request latency and gRPC status are exported as Prometheus metrics. Logs contain no tensor data.\n\n","title":"Operational notes"},{"category":"section","location":"api/manifest_boundary/#Manifest-and-Boundary","page":"Manifest & Boundary","text":"The bundle metadata types parsed from manifest.yaml, the transport-agnostic request/response boundary types, and the canonical dtype enumeration. These live in ReactantServerCore (the shared substrate). See Bundles & model.jl for the manifest format.","title":"Manifest & Boundary"},{"category":"section","location":"api/manifest_boundary/#Manifest","page":"Manifest & Boundary","text":"","title":"Manifest"},{"category":"section","location":"api/manifest_boundary/#Boundary","page":"Manifest & Boundary","text":"","title":"Boundary"},{"category":"section","location":"api/manifest_boundary/#Datatypes","page":"Manifest & Boundary","text":"","title":"Datatypes"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.Manifest","page":"Manifest & Boundary","text":"Manifest\n\nThe parsed and validated manifest.yaml of a model bundle. It records the format_version, the bundle name and description, the executable input/output specs (executable_inputs/executable_outputs), the optional client-facing specs (client_inputs/client_outputs, present only when a model.jl transforms the I/O), the BatchingSpec, provenance metadata, and the derived 0-based input_batch_dim. Tensor specs are TensorSpec values; see TensorSpec and Dim for the einsum-style shape encoding.\n\n\n\n\n\n","title":"ReactantServerCore.Manifest"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.TensorSpec","page":"Manifest & Boundary","text":"TensorSpec\n\nOne tensor in a Manifest: its name, DType, shape (a Vector{Dim}), and batch_axis (the 1-based index of the batch dim, or nothing when the tensor is not batched).\n\n\n\n\n\n","title":"ReactantServerCore.TensorSpec"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.Dim","page":"Manifest & Boundary","text":"Dim\n\nA single axis of a tensor shape. kind is one of FIXED, BATCH, or VARIABLE; size is meaningful only when kind == FIXED (it is 0 otherwise). A FIXED dim has a concrete size, a BATCH dim is the batch axis (from the reserved n/b shape letters), and a VARIABLE dim (a -1 in the manifest dims map) is a dynamic non-batch axis.\n\n\n\n\n\n","title":"ReactantServerCore.Dim"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.BatchingSpec","page":"Manifest & Boundary","text":"BatchingSpec\n\nThe set of batch sizes a bundle was compiled for (compiled_batch_sizes). At inference the request's size along the batch axis must equal one of these; the scheduler coalesces requests up to a compiled size and selects the matching executable.\n\n\n\n\n\n","title":"ReactantServerCore.BatchingSpec"},{"category":"function","location":"api/manifest_boundary/#ReactantServerCore.load_manifest","page":"Manifest & Boundary","text":"load_manifest(path) -> Manifest\n\nParse a manifest YAML file at path into a Manifest. This runs the structural parsing and validation of parse_manifest but not the bundle-directory checks in validate_manifest, so it is usable wherever only the manifest's contents are needed, for example a client deriving a model's I/O spec offline.\n\n\n\n\n\n","title":"ReactantServerCore.load_manifest"},{"category":"function","location":"api/manifest_boundary/#ReactantServerCore.is_meta","page":"Manifest & Boundary","text":"is_meta(m::Manifest) -> Bool\n\nTrue when the bundle is a meta model (a Julia orchestration over other models) rather than a regular StableHLO executable.\n\n\n\n\n\n","title":"ReactantServerCore.is_meta"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.NamedTensor","page":"Manifest & Boundary","text":"NamedTensor(name, dtype, shape, data)\nNamedTensor(name, data)\n\nA named host tensor carried across the transport boundary as both an input and an output. It pairs a tensor name with its DType, its shape (Julia column-major dimensions), and the backing data array. The two-argument form derives dtype and shape from a typed host Array.\n\n\n\n\n\n","title":"ReactantServerCore.NamedTensor"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.InferRequest","page":"Manifest & Boundary","text":"InferRequest\n\nA decoded inference request, the scheduler's unit of work. It names the target model (model_name), the requested_outputs the caller wants returned, and the input tensors (inputs, a Vector{NamedTensor}). deadline_ns is an absolute local time_ns() deadline (0 means none): a remaining-budget timeout carried over the wire is converted to this local absolute form at decode, so cross-process monotonic-clock differences never matter. The codec produces it from a wire ModelInferRequest; the scheduler and runtime consume only this transport-agnostic form.\n\n\n\n\n\n","title":"ReactantServerCore.InferRequest"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.DeadlineExceeded","page":"Manifest & Boundary","text":"DeadlineExceeded(model_name)\n\nRaised when a request's deadline has already passed at dispatch admission: the scheduler refuses to begin GPU work that is already expired, and a meta orchestration refuses to issue a further sub-call once its budget is gone. It never interrupts a running PJRT/GPU call; it only declines to start new work. The gRPC layer maps it to DEADLINE_EXCEEDED.\n\n\n\n\n\n","title":"ReactantServerCore.DeadlineExceeded"},{"category":"type","location":"api/manifest_boundary/#ReactantServerCore.DType","page":"Manifest & Boundary","text":"DType\n\nCanonical element-type enumeration shared across the server, the single source of truth for dtype translation. The companion maps convert between three representations: the manifest token form (e.g. \"f32\", \"bf16\"), the Julia element type (e.g. Float32, BFloat16), and the KServe V2 wire datatype string (e.g. \"FP32\", \"BF16\").\n\nThe DType to XLA primitive-type mapping deliberately lives in the Reactant backend, not here, so this layer carries no Reactant dependency.\n\nFP8 (F8E5M2, F8E4M3) has no standard KServe wire datatype, so those two variants are intentionally absent from the wire mapping and may appear only on executable-internal tensors, never on client-facing inputs or outputs. Conversions are performed by dtype_from_token, dtype_token, julia_type, dtype_of, dtype_size, kserve_string, and dtype_from_kserve.\n\n\n\n\n\n","title":"ReactantServerCore.DType"},{"category":"section","location":"manual/bundles/#Bundles-and-model.jl","page":"Bundles & model.jl","text":"A model is delivered to the server as a self-contained bundle: a directory holding a compiled model (an MLIR module Reactant compiles, a StableHLO program today), its weights, and a manifest. Bundles are produced offline by the conversion tooling and loaded at server startup.","title":"Bundles & model.jl"},{"category":"section","location":"manual/bundles/#Bundle-layout","page":"Bundles & model.jl","text":"A bundle is a directory containing:\n\nmanifest.yaml — the metadata parsed into a Manifest: I/O specs, dtypes, shapes, and the compiled batch sizes.\nmodel.mlir — the MLIR module Reactant compiles, currently a serialized StableHLO portable artifact (single batch size), or one module per size as model.b{N}.mlir sharing a single weights.safetensors.\nweights.safetensors — the model weights, memory-mapped at load time.\nmodel.jl — optional; registers custom pre/post-processing (see below).\n\nThe directory name is the model name and must match the manifest's name. Each immediate subdirectory of model_repo that contains a manifest.yaml is a bundle.","title":"Bundle layout"},{"category":"section","location":"manual/bundles/#Manifest-shape-encoding","page":"Bundles & model.jl","text":"Each tensor's shape is an einsum-style string of single ASCII letters, one per axis, with a companion dims: map giving the size of every non-batch letter:\n\nexecutable_inputs:\n  - name: input\n    dtype: f32\n    shape: \"chwn\"     # channel, height, width, batch\n    dims:\n      c: 3\n      h: 224\n      w: 224\n\nThe letters n and b are reserved batch markers (at most one occurrence per tensor). Other letters are tensor-scoped (no implicit cross-tensor equality) and must be unique within a single shape. A size of -1 in dims marks a variable axis (used today only in client_outputs that pass through model.jl). The per-input batch axis is derived from the position of n/b; at inference the request's size along that axis must equal one of batching.compiled_batch_sizes. Each tensor parses into a TensorSpec with a Dim per axis, and the compiled sizes form the BatchingSpec.\n\nShapes use the Julia column-major convention (the batch dimension is the last axis), which is the reverse of the row-major form a Python/XLA exporter would write. The wire codec handles the conversion, so KServe clients see canonical row-major shapes.\n\nDatatypes are written as manifest tokens (f32, bf16, i64, bool, and so on); see DType for the full mapping between tokens, Julia types, and KServe wire strings. Client-facing tensors must use a dtype that has a KServe wire mapping (FP8 is executable-only).","title":"Manifest shape encoding"},{"category":"section","location":"manual/bundles/#Custom-pre/post-processing-with-model.jl","page":"Bundles & model.jl","text":"A bundle may include a model.jl that calls register_model to attach preprocess and postprocess hooks. Both hooks receive and return a Vector{NamedTensor} (see NamedTensor); omitted hooks default to identity.\n\n# model.jl, inside the bundle directory\nusing ReactantServer\n\nfunction normalize(inputs)\n    # inputs :: Vector{NamedTensor}; transform and return a Vector{NamedTensor}\n    return inputs\nend\n\nfunction to_classes(outputs)\n    # e.g. map logits to class ids\n    return outputs\nend\n\nregister_model(\"resnet50\"; preprocess=normalize, postprocess=to_classes)\n\nThe worker runs the hooks on each request's own task (preprocess before the request is queued, postprocess on the result), crossing the world-age boundary with invokelatest. This means the hooks for different requests run concurrently, on multiple threads, overlapping the GPU execution: keep them free of shared mutable state (or guard it yourself). When model.jl transforms the I/O, declare the client-facing tensors via client_inputs / client_outputs in the manifest; without a model.jl those keys are not permitted and the executable specs are the client-facing specs. See register_model in the API reference for the exact hook signatures.\n\nFor a bundle whose model.jl chains several models with data-dependent logic rather than wrapping one executable, see Meta Models.","title":"Custom pre/post-processing with model.jl"},{"category":"section","location":"manual/bundles/#Producing-bundles","page":"Bundles & model.jl","text":"ReactantServerExport produces bundles offline and is kept out of the server's dependency graph. It is not part of the server runtime.\n\nA project that owns a Lux model (or any Reactant-traceable function) uses ReactantServerExport; Lux itself is not a dependency of the package:\n\nusing ReactantServerExport\nexport_bundle(:lux, model, ps, st, example_input;\n    dir=\"bundles/mlp\", name=\"mlp\", batch_sizes=[1, 8])\n\nA PyTorch project also loads PythonCall, which triggers the package extension driving torch.export.export and torchax:\n\nusing ReactantServerExport, PythonCall\nexport_bundle(:pytorch, model, (example_input,);\n    dir=\"bundles/mlp\", name=\"mlp\", batch_sizes=[1, 8])\n\nBoth frontends trace once per requested batch size and write a server-loadable bundle. The batch dimension is the last Julia axis (the leading PyTorch axis after the row-major / column-major reversal). The test suite also builds small bundles directly; see test/stablehlo_fixtures.jl.\n\n","title":"Producing bundles"},{"category":"section","location":"api/scheduling/#Scheduling","page":"Scheduling","text":"The dispatch engine that decides which model runs next and coalesces same-model requests. See Architecture for the decision policy.\n\n","title":"Scheduling"},{"category":"type","location":"api/scheduling/#ReactantServer.Scheduler","page":"Scheduling","text":"Scheduler\n\nThe deficit-weighted, cost-aware, coalescing dispatch engine. Concurrent requests land on per-model queues; a single dispatch loop runs one GPU execution at a time, coalescing same-model requests into one batched execution at a compiled size and sharing GPU time by relative model weight and a learned per-batch-size cost estimate. It holds the model registry, the backend, the device memory pool, the SchedulerConfig, per-model dispatch state, and an optional on-demand WeightCache. Submit work with infer and read observability counters with scheduler_metrics.\n\n\n\n\n\n","title":"ReactantServer.Scheduler"},{"category":"function","location":"api/scheduling/#ReactantServer.infer","page":"Scheduling","text":"infer(scheduler, request) -> Vector{NamedTensor}\n\nSubmit a request and block until the scheduler returns the result. Re-raises any error captured during dispatch.\n\nRuns the model's preprocess/postprocess hooks here, on the caller's task, rather than on the dispatch loop: preprocess before the request is queued, postprocess on the raw device outputs the loop hands back. Because each gRPC request runs on its own task, many requests' hook work proceeds in parallel and overlaps the single, serialized GPU execution. The dispatch loop coalesces and runs qr.prepared and never executes a request whose preprocess has not finished, since a request is only enqueued (made visible to the loop) after preprocess returns.\n\n\n\n\n\n","title":"ReactantServer.infer"},{"category":"function","location":"api/scheduling/#ReactantServer.scheduler_metrics","page":"Scheduling","text":"scheduler_metrics(scheduler) -> Dict{String,NamedTuple}\n\nSnapshot per-model observability: dispatch count, total compute consumed, current recent-compute EMA, queue-wait P50/P99, the histogram of dispatch batch sizes, and residency.\n\n\n\n\n\n","title":"ReactantServer.scheduler_metrics"},{"category":"section","location":"manual/node_config/#Node-Configuration","page":"Node Configuration","text":"A deployment is described by a single node file. It is the only supported config format. It describes one or more single-GPU workers on one machine and, optionally, the gateway that fronts them. Each worker reads this same file, resolves its own entry by name, and loads (and can serve) every bundle in the shared model repository.\n\nA single GPU is just a one-worker node: keep one entry under workers: (or omit workers: entirely under the supervisor, below). Growing to more GPUs means adding workers, not changing the config format.\n\nUnder the node supervisor (the container default, see Docker Deployment) the workers: list is optional: omit it and add gpus: auto (or an integer count, or an explicit device list) and the supervisor synthesizes one worker per detected GPU, assigning each its device. An explicit workers: list still wins when present. The keys below describe that explicit form, which the supervisor also honors.","title":"Node Configuration"},{"category":"section","location":"manual/node_config/#Top-level-keys","page":"Node Configuration","text":"# One shared bundle repository. Each immediate subdirectory containing a manifest.yaml is a\n# bundle; its directory name is the model name.\nmodel_repo: /var/lib/reactantserver/models\n\n# Worker at index i binds base_port + i unless it sets an explicit `port:`.\nbase_port: 8080\n\nglobal:    # defaults merged into every worker (any block may be overridden per worker)\nworkers:   # one entry per GPU\nmodels:    # optional; pins the named models to device memory on the listed workers\ngateway:   # optional; read only by the gateway, never by a worker","title":"Top-level keys"},{"category":"section","location":"manual/node_config/#Global-settings","page":"Node Configuration","text":"The global: block holds defaults merged into every worker; a worker entry may override any of these blocks. The sub-blocks map onto the resolved ServerConfig:\n\nglobal:\n  cache_dir: /var/cache/reactantserver\n  model_control_mode: dynamic  # dynamic (watch the repo) | static | explicit (control plane)\n  model_poll_seconds: 15.0     # repository poll interval in dynamic mode\n  runtime:                 # -> RuntimeConfig\n    backend: cuda          # cpu or cuda\n    mem_fraction: 0.9      # fraction of device memory claimed for the pool (GPU only)\n    preallocate: true      # claim the pool up front (GPU only)\n    allow_cpu_fallback: false\n    weight_cache_bytes: 0  # GPU byte budget for on-demand weights; 0 keeps all resident\n  scheduler:               # -> SchedulerConfig\n    discipline: fair       # fair | fifo | edf (use fifo or edf behind a gateway running lpt_packing)\n    ema_halflife_seconds: 30.0\n    max_queue_depth: 1024  # per-model queue cap; a full model rejects new requests\n    dispatch_timeout_seconds: 30.0\n    models: {}             # per-model overrides -> ModelSchedConfig (see below)\n  endpoints:               # -> EndpointsConfig\n    host: 0.0.0.0          # bind all interfaces so the gateway/clients can reach the worker\n    max_concurrent_requests: 64  # in-flight RPC cap; 0 = uncapped. Past the cap the worker sheds\n                                 # with RESOURCE_EXHAUSTED. Keep it above the gateway's per-worker\n                                 # outbound stream limit (worker_client.max_concurrent_streams)\n\nmodel_control_mode sets how the loaded model set evolves: dynamic (the default) watches the repository and loads, unloads, and reloads bundles online as files change; static fixes the startup set; explicit cedes the lifecycle to an external control plane via the worker control RPCs. scheduler.discipline selects the dispatch policy: fair shares GPU time across models by weighted deficit and learned cost, while fifo serves the oldest queued request first. Workers fronted by a gateway in lpt_packing mode must run fifo or edf (not fair), so the gateway stays the placement and fairness authority (see Multi-GPU Gateway).\n\nedf (earliest-deadline-first) serves the model whose most-urgent queued request has the soonest deadline, where the deadline comes from the request's remaining-budget timeout. A meta model is not scheduled here (it runs on the request task), but each of its in-flight sub-calls inherits the meta's deadline, so under edf a meta's continuation is ordered ahead of fresher regular work. It is designed for deadline-sensitive serving: while every client uses the same deadline it behaves exactly like fifo, and it diverges only to dispatch requests with less budget left ahead of those with more, so a request close to its deadline is served before a fresher one rather than missing behind it. edf also sheds work it cannot finish within its learned compute cost (laxity), trading some throughput (batch fragmentation, and no per-model weighting) for meeting more deadlines under load. Note that because edf derives urgency solely from the deadline, issuing different per-client deadlines for the same model reorders that model's service and therefore affects fairness across clients; keep deadlines uniform to retain fifo-like fairness.\n\nEach sub-block corresponds to a typed config struct: RuntimeConfig, SchedulerConfig, ModelSchedConfig, and EndpointsConfig. See the API Reference for every field and its default.","title":"Global settings"},{"category":"section","location":"manual/node_config/#Workers","page":"Node Configuration","text":"workers:\n  - { name: worker0 }\n  - { name: worker1 }\n\nname is the routing identity (and, under Docker, the compose service name). The listen port is base_port + index unless the worker sets an explicit port:.\n\nUnder the supervisor (the container default) you do not assign GPUs yourself: it detects the visible devices, gives each worker one of them, and sets that worker's own CUDA_VISIBLE_DEVICES, so every worker sees a single GPU at ordinal 0. Influence the assignment with gpus: above (auto, a count, or an explicit device list), the REACTANT_GPUS environment variable, or by adding gpu: N to a worker entry to pin it to a specific visible device. A container-level CUDA_VISIBLE_DEVICES acts as a coarse filter on which physical GPUs the supervisor sees (see Docker Deployment). Running a single worker by hand without the supervisor (a bare serve), the worker uses device ordinal 0, or gpu: N to pick another.\n\nA worker entry may also carry override blocks (for example a runtime: block) that merge over global.","title":"Workers"},{"category":"section","location":"manual/node_config/#Device-pinning-(the-models:-map)","page":"Node Configuration","text":"models:\n  resnet50: [worker0, worker1]   # hot on both GPUs\n  vsq_coral: [worker0]           # hot on worker0 only\n\nEvery worker loads (and can serve) every bundle in model_repo; the gateway discovers what each worker serves and schedules requests across them. The optional top-level models: map is a per-model override that pins the named models to device memory on the listed workers for the lowest latency (it translates into scheduler.models.<name>.residency: device on those workers). Unlisted models stay system-pinned in host RAM and load to the device on demand. Omit the block entirely to keep every model on-demand. To load only a subset of the repository on a worker, the resolved config also supports a models_include allowlist (empty means load all).","title":"Device pinning (the models: map)"},{"category":"section","location":"manual/node_config/#Per-model-scheduler-overrides","page":"Node Configuration","text":"Tune individual models under scheduler.models, which builds the ModelSchedConfig entries:\n\nscheduler:\n  models:\n    resnet50:\n      weight: 2.0                  # relative compute share (default 1.0)\n      residency: device            # keep weights GPU-resident for the server's lifetime\n      max_batch_size: 8            # cap on rows coalesced per dispatch (default uncapped)\n    yolo:\n      residency: unpinned          # no host floor; re-materialized from disk on each load\n\nweight sets the model's fair share. residency sets the model's residency floor (unpinned, system, or device; pin_to_gpu: true is a back-compat alias for residency: device). When the on-demand weight cache is enabled, models without an explicit residency default to system (weights pinned in host RAM); see On-demand Weights.\n\nmax_batch_size caps how many rows the scheduler coalesces into one dispatch of the model. It does not change compiled shapes: the dispatch sizes come from the batch sizes the bundle was compiled for (a partial fill still pads up to the smallest compiled size), the batch axis comes from the bundle manifest, and a single request larger than the cap is still served in one dispatch because requests are never split.","title":"Per-model scheduler overrides"},{"category":"section","location":"manual/node_config/#Gateway-configuration","page":"Node Configuration","text":"The gateway does not read the node file at all: it is configured by its own standalone gateway.yml, which carries its listen addresses, limits, and a flat endpoints: list of worker host:port addresses (see Multi-GPU Gateway). A gateway.yml looks like:\n\nlisten:\n  grpc: \"0.0.0.0:8001\"\n  metrics: \"0.0.0.0:8002\"\ngrpc:\n  max_recv_msg_bytes: 268435456   # 256 MiB\n  max_send_msg_bytes: 268435456\n  max_concurrent_requests_per_worker: 64   # inbound cap is this x worker count; 0 = uncapped.\n                                           # Sized above the outbound stream limit so a startup\n                                           # burst has headroom rather than being shed early\nworker_client:\n  request_timeout_seconds: 60\n  max_concurrent_streams: 32      # outbound in-flight RPCs the gateway multiplexes to one worker\nlogging:\n  level: \"info\"\n  format: \"json\"\nscheduling:\n  mode: round_robin               # round_robin | lpt_packing (see Multi-GPU Gateway)\nendpoints:                        # worker host:port addresses, across any number of nodes\n  - \"worker0:8080\"\n  - \"worker1:8081\"","title":"Gateway configuration"},{"category":"section","location":"manual/node_config/#Environment-variable-overrides","page":"Node Configuration","text":"Any worker value can be overridden per process by an environment variable of the form INFERENCE_SERVER_<SECTION>_<FIELD>, for example:\n\nINFERENCE_SERVER_ENDPOINTS_PORT=9100\nINFERENCE_SERVER_RUNTIME_BACKEND=cpu\nINFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_BYTES=8589934592\n\nList-valued overrides (INFERENCE_SERVER_MODEL_DIRS, INFERENCE_SERVER_MODELS_INCLUDE) are colon-separated. Overrides are applied on top of the resolved node config, and the effective configuration is logged at startup.\n\n","title":"Environment-variable overrides"},{"category":"section","location":"manual/scaling/#Scaling-to-Multiple-GPUs","page":"Scaling to Multiple GPUs","text":"Getting Started ran one model on one GPU. This page adds GPUs. The key point: nothing about your model, client, or external interface changes. You run the same container (or the same supervise call), the supervisor puts one worker on each GPU, and it fronts them with an embedded gateway so clients still send one request to one endpoint and it is routed to a worker that serves the model.\n\nFor help deciding between the distributed (memory-constrained) and replicated (compute-constrained) multi-GPU shapes, with an example configuration for each, see Common Use Cases.","title":"Scaling to Multiple GPUs"},{"category":"section","location":"manual/scaling/#The-node-file-is-unchanged","page":"Scaling to Multiple GPUs","text":"gpus: auto already means \"one worker per visible GPU\", so the single-GPU node file from Getting Started scales as-is: give the host more GPUs and the supervisor runs more workers. You only edit the node file to do something non-default:\n\nmodel_repo: /var/lib/reactantserver/models\nbase_port: 8080           # worker i listens on base_port + i (8080, 8081, ...)\nmetrics_base_port: 9100   # and metrics on metrics_base_port + i (9100, 9101, ...)\ngpus: auto                # or an integer count, or an explicit device list\n\nglobal:\n  runtime:\n    backend: cuda\n  endpoints:\n    host: 0.0.0.0\n\n# Optional: replicate or pin specific models to device memory on specific workers.\n# models:\n#   mlp: [worker0, worker1]   # served (and kept device-resident) on both GPUs\n\nA model listed on more than one worker is replicated, and the gateway load-balances requests for it across those workers. See Node Configuration for the models: map and On-demand Weights for fitting more models than GPU memory holds.","title":"The node file is unchanged"},{"category":"section","location":"manual/scaling/#Run-it-with-docker-compose","page":"Scaling to Multiple GPUs","text":"Same container as before; just grant it all the GPUs (the shipped docker-compose.yml already reserves every GPU):\n\nREACTANTSERVER_MODELS=$PWD/models docker compose up\n\nThe supervisor detects N GPUs, runs worker0..workerN-1 (each pinned to one device), and runs the embedded gateway on localhost:8001 (gRPC) and localhost:8002 (metrics). Clients are unchanged from Getting Started: they still call 8001 with the model name, and the gateway routes each request to a worker serving that model.","title":"Run it with docker compose"},{"category":"section","location":"manual/scaling/#Run-it-from-pure-Julia","page":"Scaling to Multiple GPUs","text":"One supervise call on the multi-GPU host does the whole fan-out, just as it does for one GPU:\n\nusing ReactantServerNode\nReactantServerNode.supervise(\"node.yaml\")   # one worker per GPU + the embedded gateway\n\nThis single parent process spawns one ReactantServer.serve worker subprocess per GPU and the gateway as another subprocess, multiplexes their logs onto its stdout with [worker0] / [gateway] prefixes, and restarts any child that dies. (You can still run each worker and the gateway by hand, as separate serve / serve_gateway processes, but the supervisor is the intended path and the only one the container uses.)","title":"Run it from pure Julia"},{"category":"section","location":"manual/scaling/#How-the-supervisor-decides-what-to-start","page":"Scaling to Multiple GPUs","text":"The supervisor's job, on startup, is to turn \"this host + this node file\" into a concrete set of child processes. It does so in three steps.\n\n1. Detect the devices. The first of these that yields a non-empty answer wins (ReactantServerNode.detect_gpus):\n\nREACTANT_GPUS environment variable: a count (2) or an explicit list (0,2 or GPU UUIDs); 0 means a CPU node.\nthe node file's gpus: key (auto, a count, or a list).\na CUDA_VISIBLE_DEVICES already set on the container.\nnvidia-smi enumeration.\n/dev/nvidiaN device nodes.\n\nFor a CUDA node that finds no devices, startup fails with guidance (run with --gpus all, set REACTANT_GPUS, or set backend: cpu).\n\n2. Materialize the workers (ReactantServerCore.materialize_node!). With no workers: list, one worker is synthesized per detected device (worker0..workerN-1). With an explicit list, each worker is assigned a device positionally (or by its gpu: key). Either way each worker is pinned to exactly one device through its own CUDA_VISIBLE_DEVICES, so inside the worker the device is always ordinal 0 and the single-GPU worker code runs unchanged. The materialized node file is written to /run/reactantserver/node.yaml for inspection.\n\nEach worker's compute-thread pool is sized to its share of the host: min(CPU_THREADS ÷ workers, 16) threads, plus one interactive thread for the GPU dispatch loop. This avoids the oversubscription that --threads=auto would cause — with several workers on one node, each auto worker (and its GC and host library pools) would otherwise size itself to the whole machine and the workers would fight for every core under load. The cap keeps a very large box from handing any one worker an unhelpfully huge pool. Set REACTANT_WORKER_THREADS to override the computed value (it is used verbatim, bypassing the split and the cap). Sized this way, each worker's steady-state CPU should sit near its actual working set (dispatch, GPU sync, and the data plane), not all cores.\n\n3. Decide on the gateway by worker count, in the default all role:\n\nOne worker → no gateway. A lone worker already serves the full KServe V2 API, so the supervisor binds it directly to the public ports (8001/8002) and starts no gateway. No extra process, no extra hop.\nTwo or more workers → workers plus the embedded gateway. Each worker binds base_port + i (and metrics_base_port + i), and the gateway binds the public 8001/8002. The supervisor synthesizes the gateway's worker list (and worker metrics list) from the node file, so the gateway needs no config of its own.\n\nThe external interface (8001 for gRPC, 8002 for metrics/health) is therefore identical whether you run 1 GPU or 8, which is why your client and compose ports never change as you scale.\n\nThe REACTANT_ROLE environment variable can override the default all role (workers runs only the workers, gateway runs only the gateway) for splitting a deployment across machines; that multi-node topology is beyond the scope of these guides.","title":"How the supervisor decides what to start"},{"category":"section","location":"manual/scaling/#Watch-it-without-a-GPU","page":"Scaling to Multiple GPUs","text":"To see the decision logic and the prefixed logs on a machine with no GPU, run the supervisor as a CPU node with two synthetic workers:\n\nREACTANT_GPUS=0 REACTANT_CPU_WORKERS=2 \\\n  julia --project=packages/ReactantServerNode -e 'using ReactantServerNode; ReactantServerNode.supervise(\"node.yaml\")'\n\nwith backend: cpu in the node file. You will see worker0, worker1, and gateway start, their logs interleaved with [name] prefixes, and the gateway serving on 8001/8002, exactly the multi-worker shape it takes on a multi-GPU host.","title":"Watch it without a GPU"},{"category":"section","location":"manual/scaling/#Metrics","page":"Scaling to Multiple GPUs","text":"One scrape on 8002 still covers the whole node: the embedded gateway serves its own gateway_* series and merges in every worker's /metrics. Each worker tags its series with worker and gpu labels itself (the gpu value is the physical device behind its CUDA_VISIBLE_DEVICES), so per-GPU and per-worker breakdowns need no Prometheus relabeling, e.g. sum by (gpu) (rate(worker_dispatch_total[1m])). See Docker Deployment for the scrape config.\n\n","title":"Metrics"},{"category":"section","location":"#ReactantServer.jl","page":"Home","text":"A production inference server that serves models compiled through Reactant.jl — StableHLO via XLA today — from a single Julia process, built on Reactant's PJRT bindings. It targets static-graph workloads (computer vision, scientific computing) where many models share one GPU and only one model executes at a time. To serve more models than fit in GPU memory at once, it keeps every model's weights resident in host RAM and transfers them onto the GPU on demand, evicting cold models under a memory budget. It speaks the KServe V2 inference API natively over gRPC, so standard Triton and KServe clients connect to it directly.","title":"ReactantServer.jl"},{"category":"section","location":"#What-works-today","page":"Home","text":"StableHLO bundle loading, manifest parsing and validation, and a typed YAML configuration with environment-variable overrides.\nThe Reactant/PJRT runtime: deserialize a portable artifact, compile with weights bound as explicit arguments, execute, and read results back. A single shared memory pool backs all models.\nOn-demand GPU weight loading with a host-RAM weight cache (see On-demand Weights), which decouples the number of servable models from GPU memory capacity.\nA deficit-weighted, cost-aware, coalescing Scheduler that runs one GPU execution at a time and batches same-model requests at compiled sizes.\nThe KServe V2 control plane over gRPC: liveness/readiness, model and server metadata, inference, a RepositoryIndex, and the Triton-compatible system shared-memory data plane.\nModel lifecycle control: the default dynamic mode watches the model repository and loads/unloads/reloads bundles online (weights, MLIR, and model.jl changes alike); static fixes the startup set; explicit cedes lifecycle and residency to an external control plane over the worker control RPCs.\nMulti-GPU scheduling in the gateway: round_robin or lpt_packing, which places each model on a fixed, operator-configured number of GPUs and routes its requests to fill one replica's batch before the next, preserving batch coalescing (see Multi-GPU Gateway).\nMultiple compiled batch sizes per model and custom per-model pre/post-processing via a bundle's model.jl (see Bundles & model.jl).\n\nReactantServer.jl is a Julia workspace of four packages: ReactantServerCore (the shared, Reactant-free substrate), ReactantServer (the worker, the only package that loads Reactant), ReactantServerGateway (the multi-GPU reverse proxy), and ReactantServerClient (a Reactant-free inference client), plus the non-member ReactantServerExport (offline bundle export). See Architecture for the split.","title":"What works today"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"New here? Start with Getting Started.\nChoosing a deployment shape (single GPU, multi-GPU, multi-node) with example configs: Common Use Cases.\nCalling a server from your code: Client Usage.\nConfiguring a deployment: Node Configuration.\nScaling to multiple GPUs: Scaling to Multiple GPUs.\nPackaging a model: Bundles & model.jl.\nServing more models than fit on the GPU: On-demand Weights.\nScaling across GPUs: Multi-GPU Gateway and Docker Deployment.\nThe why and the how: Philosophy and Architecture.\nThe full API Reference.","title":"Where to go next"},{"category":"section","location":"#Quick-start","page":"Home","text":"using ReactantServer\nReactantServer.serve(\"docker/node.yaml\")                    # single worker: name optional\nReactantServer.serve(\"docker/node.yaml\"; worker=\"worker0\")  # multi-worker: name the worker\n\nAll tests run on CPU and need no GPU. Each package is tested in its own environment:\n\njulia --project=packages/ReactantServerCore   -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServer        -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServerGateway -e 'using Pkg; Pkg.test()'\njulia --project=packages/ReactantServerClient  -e 'using Pkg; Pkg.test()'\n\n","title":"Quick start"},{"category":"section","location":"manual/on_demand_weights/#On-demand-Weights","page":"On-demand Weights","text":"Because only one model executes at a time, the GPU does not need every model's weights resident simultaneously. This lets a GPU sized for a handful of models serve a much larger catalog, paying a small transfer cost only when a cold model is first called. The Architecture page covers the rationale; this page is the operational guide.","title":"On-demand Weights"},{"category":"section","location":"manual/on_demand_weights/#Two-tiers-of-weight-memory","page":"On-demand Weights","text":"Host RAM (resident by default). With the on-demand cache enabled, every model's weights default to system-pinned (residency: system): materialized once from disk into host RAM at startup and kept there. Host memory is plentiful and cheap relative to GPU memory, which removes disk from the hot path. A model can opt out with residency: unpinned, paying a mmap re-materialization on each on-demand load instead.\nGPU (managed working set). Pinned models keep their weights on the GPU for the server's lifetime. Every other model is loaded onto the GPU on demand when a request arrives, kept resident afterward so repeat requests are free, and evicted under a configured GPU byte budget using a least-recently-used policy. Eviction frees the device memory immediately through an explicit PJRT buffer release.\n\nBecause the weights are already in RAM, an on-demand GPU load is a single host-to-device transfer rather than a reload from disk: tens of milliseconds even for the largest models, the same order of magnitude as a single inference.","title":"Two tiers of weight memory"},{"category":"section","location":"manual/on_demand_weights/#Enabling-it","page":"On-demand Weights","text":"Set the GPU byte budget for on-demand (unpinned) weights via runtime.weight_cache_bytes. A value of 0 keeps every model resident (the original behavior); any positive value enables the on-demand cache and bounds the device memory used for unpinned models.\n\nglobal:\n  runtime:\n    backend: cuda\n    weight_cache_bytes: 8589934592   # 8 GiB budget for on-demand weights\n\nThis is the weight_cache_bytes field of RuntimeConfig. It can also be set with the INFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_BYTES environment variable.","title":"Enabling it"},{"category":"section","location":"manual/on_demand_weights/#Pinning-hot-models","page":"On-demand Weights","text":"A model that must never pay the on-demand transfer cost can be pinned to stay GPU-resident for the server's lifetime. Pinned models are exempt from eviction; the budget bounds only the unpinned working set.\n\nscheduler:\n  models:\n    resnet50:\n      residency: device      # pin_to_gpu: true is a back-compat alias\n\nThis is the residency field of ModelSchedConfig (unpinned, system, or device; unspecified models default to system when the cache is enabled). Pin the latency-sensitive or highest-traffic models, and let the long tail load on demand.","title":"Pinning hot models"},{"category":"section","location":"manual/on_demand_weights/#Sharing-host-weights-across-same-node-workers","page":"On-demand Weights","text":"With several workers on one node, runtime.shared_host_weights: true backs each system-pinned model's host copy with a node-shared POSIX shared-memory region so the workers share one copy. The regions and their lock files are created with mode 666 by default so containers running as unrelated UIDs can share them; that is world-writable, so set runtime.shared_host_weights_mode: \"660\" on production and multi-user systems (the server warns at startup when the shared store runs with the 666 default).","title":"Sharing host weights across same-node workers"},{"category":"section","location":"manual/on_demand_weights/#Observability","page":"On-demand Weights","text":"The Scheduler exposes weight-cache residency and load/evict counters alongside its dispatch metrics; read them with scheduler_metrics. Use them to confirm that hot models stay resident and that the eviction rate is acceptable for your budget. Coalescing (packing many requests into one execution) amortizes a one-time on-demand transfer across every item in the batch, so on-demand loading and batching reinforce each other.\n\n","title":"Observability"}]
}
